FOLLOW THESE INSTRUCTIONS FOR EACH ITERATION. DO NOT CHANGE THIS FILE

1) Read `AGENTS.md` if you have not already. Learn how to run the application when needed. 

2) before starting, check what is untracked or added to git. Make atomic commits that make sense before starting this task.

2) Open `implementation_plan.md`, go directly to the current section number at the top of the file. First read the task in that section, the context, where to look potentially, and How to test / what success looks like. We are doing test-driven dev where we need to work backward from what is required to succeed, and write the code accordingly. Check how things are currently done to not duplicate work. Now start. As you make changes, commit atomically and push as you go. 

3) Once all the tests pass, all code in this section was committed and pushed, then Increment the current section number in `implementation_plan.md` by one only if the section is fully implemented and thoroughly tested.  

4) when you finish, put a check mark next to the section you just did with a note within that section of what you did
