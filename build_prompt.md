FOLLOW THESE INSTRUCTIONS FOR EACH ITERATION. DO NOT CHANGE THIS FILE

1) Read `AGENTS.md` if you have not already. Learn how to run the application when needed.  
2) Review the codebase structure and existing components to avoid duplication when given a task.  
3) Open `implementation_plan.md`, go directly to the current section number at the top of the file, and execute that section immediately. Do not stop after summarizing files or constraints.  
4) Implement every requirement in that section and run the listed tests. Use the success criteria described in the section's `How to Test` text to decide whether the section is actually complete.  
5) Increment the current section number in `implementation_plan.md` by one only if the section is fully implemented and thoroughly tested.  
6) Commit and push the current changes with a fitting name. **have multiple commits per iteration** If there is a lot to commit, split it up into atomic commits that are smaller. make the name clear what each task was about.
7) when you finish, put a check mark next to the section you just did with a note within that section of what you did
