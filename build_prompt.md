# Build Prompt

1) Read `AGENTS.md` if you have not already. Learn how to run the application when needed.  
2) Review the codebase structure and existing components to avoid duplication when given a task.  
3) Check the current section number at the top of `implementation_plan.md`. Execute every step in that section. You will be given a task and must implement it and verify it works.  
4) Increment the current section number in `implementation_plan.md` by one.  
5) Commit and push the current changes with a fitting name.
