---
phase: 04-frontend-prompt-execution-surface
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/frontend/contracts.ts
  - src/frontend/routes/runs.ts
  - src/frontend/client/api-client.ts
  - src/frontend/client/state.ts
  - src/tests/frontend/state.test.ts
  - src/tests/frontend-api/runs.stream.test.ts
autonomous: true
must_haves:
  truths:
    - "Frontend can consume typed run events from SSE without crashing on malformed frames."
    - "Run state updates are idempotent and monotonic for duplicated or out-of-order tool events."
    - "Event stream lifecycle closes cleanly on terminal events and when a new run supersedes the old stream."
  artifacts:
    - path: "src/frontend/contracts.ts"
      provides: "Typed SSE event schemas for run_state, tool_call, run_complete, run_error"
      contains: "Tool name enum locked to web_search and web_crawl"
    - path: "src/frontend/client/api-client.ts"
      provides: "EventSource subscription lifecycle with typed event parsing"
    - path: "src/frontend/client/state.ts"
      provides: "Idempotent reducer updates keyed by toolCallId with terminal-state guards"
    - path: "src/tests/frontend/state.test.ts"
      provides: "Reducer invariants for ordering, dedupe, and terminal-state guards"
  key_links:
    - from: "src/frontend/client/api-client.ts"
      to: "/api/runs/:runId/events"
      via: "EventSource stream subscription"
      pattern: "new EventSource"
    - from: "src/frontend/client/api-client.ts"
      to: "src/frontend/client/state.ts"
      via: "typed stream callbacks dispatch reducer events"
      pattern: "toolCallId"
---

<objective>
Implement the backend-to-frontend streaming foundation for live run progress by adding strict event contracts, SSE parsing, and deterministic reducer behavior.

Purpose: Reduce integration risk before UI rendering by locking event shape and state-transition correctness first.
Output: Typed SSE stream ingestion and reducer invariants that safely handle duplication, ordering races, and terminal lifecycle boundaries.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-prompt-execution-surface/04-RESEARCH.md
@.planning/phases/04-frontend-prompt-execution-surface/04-01-PLAN.md
@src/frontend/contracts.ts
@src/frontend/routes/runs.ts
@src/frontend/client/api-client.ts
@src/frontend/client/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run-event contracts and SSE stream plumbing</name>
  <files>src/frontend/contracts.ts, src/frontend/routes/runs.ts, src/frontend/client/api-client.ts, src/tests/frontend-api/runs.stream.test.ts</files>
  <action>Extend contracts with typed run-event schemas for `run_state`, `tool_call`, `run_complete`, and `run_error`, including strict enum lock for tool names (`web_search`, `web_crawl`) and nonnegative `durationMs`. Implement frontend stream client subscription to `GET /api/runs/:runId/events` via `EventSource`, including event-type handlers, parse/validation guards, and explicit `close()` behavior on terminal events and reruns. Update run route support only as needed to expose consistent SSE event names/payload shape (do not add full payload observability reserved for Phase 5). Add stream contract tests that feed representative event frames and assert parser/handler behavior and close semantics.</action>
  <verify>npm run test -- src/tests/frontend-api/runs.stream.test.ts</verify>
  <done>Frontend can open a typed SSE stream for a started run, parse all required event types, and close stream resources correctly on completion/error/new-run.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce idempotent reducer updates for stream events</name>
  <files>src/frontend/client/state.ts, src/tests/frontend/state.test.ts</files>
  <action>Implement reducer transitions for stream events with monotonic run-phase rules and idempotent tool updates keyed by `toolCallId`. Ensure out-of-order duplicate events merge safely instead of creating duplicate records, and terminal phases reject regressions from subsequent stale events. Keep state shape compatible with a separate rendering plan by exposing tool-call records + timestamps needed for timeline projection without adding UI formatting logic here.</action>
  <verify>npm run test -- src/tests/frontend/state.test.ts</verify>
  <done>Reducer guarantees stable run/tool state under event reordering/duplication and provides deterministic data ready for timeline rendering in the next plan.</done>
</task>

</tasks>

<verification>
1. Run stream contract tests to validate SSE parsing and lifecycle closure paths.
2. Run reducer tests to validate dedupe, out-of-order handling, and terminal-state regression guards.
3. Confirm state outputs include normalized tool-call records consumable by timeline rendering.
</verification>

<success_criteria>
- SSE event ingestion is schema-validated and stable for all required run event types.
- Reducer logic is race-safe and idempotent across duplicate/out-of-order tool events.
- 04-03 can focus only on UI rendering because stream and state foundations are already verified.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-prompt-execution-surface/04-02-SUMMARY.md`
</output>
