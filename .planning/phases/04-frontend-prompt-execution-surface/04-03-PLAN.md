---
phase: 04-frontend-prompt-execution-surface
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/frontend/client/timeline.ts
  - src/frontend/client/app.ts
  - public/index.html
  - src/tests/frontend/timeline.test.ts
autonomous: true
must_haves:
  truths:
    - "User sees each tool call (`web_search`, `web_crawl`) with live status in the timeline while a run is active."
    - "User sees duration for each tool call, with backend `durationMs` taking precedence once a call completes."
    - "UI reaches and reflects stable terminal state on `run_complete` or `run_error` without duplicated timeline rows across reruns."
  artifacts:
    - path: "src/frontend/client/timeline.ts"
      provides: "Projection helpers that convert reducer tool-call state into render-ready rows and duration labels"
    - path: "src/frontend/client/app.ts"
      provides: "Timeline rendering + terminal-state sync wiring to existing run form and stream lifecycle"
    - path: "public/index.html"
      provides: "Dedicated timeline render target in minimal frontend layout"
    - path: "src/tests/frontend/timeline.test.ts"
      provides: "Timeline projection and duration-precedence tests"
  key_links:
    - from: "src/frontend/client/app.ts"
      to: "src/frontend/client/timeline.ts"
      via: "render pipeline maps reducer data to timeline rows"
      pattern: "toTimelineRows|timeline"
    - from: "src/frontend/client/app.ts"
      to: "public/index.html"
      via: "DOM target updates for tool timeline and terminal summary"
      pattern: "timeline"
    - from: "src/frontend/client/timeline.ts"
      to: "src/frontend/client/state.ts"
      via: "consumes normalized tool-call records keyed by toolCallId"
      pattern: "toolCallId"
---

<objective>
Render the live per-tool timeline UI using the stream/state foundation from 04-02 so users can track tool status and duration through run completion.

Purpose: Close Phase 04 user-visible progress requirements while keeping implementation scope tightly focused on rendering and terminal-state UX sync.
Output: Minimal timeline UI showing tool rows, status badges, and duration values with correct running/finalized behavior.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-prompt-execution-surface/04-RESEARCH.md
@.planning/phases/04-frontend-prompt-execution-surface/04-02-PLAN.md
@src/frontend/client/state.ts
@src/frontend/client/app.ts
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build timeline projection and duration precedence helpers</name>
  <files>src/frontend/client/timeline.ts, src/tests/frontend/timeline.test.ts</files>
  <action>Create timeline projection helpers that derive ordered, render-ready rows from reducer tool-call records keyed by `toolCallId`. Implement duration logic that shows provisional elapsed display while a call is running, then switches to backend-provided `durationMs` once terminal call data exists. Add focused tests for ordering stability, duration precedence, and formatting across running/completed/error cases.</action>
  <verify>npm run test -- src/tests/frontend/timeline.test.ts</verify>
  <done>Timeline helpers produce deterministic rows with correct duration behavior and test coverage for running/final transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Render timeline rows and terminal run outcomes in the app shell</name>
  <files>src/frontend/client/app.ts, public/index.html</files>
  <action>Update the minimal frontend shell to include a timeline region and render each tool row with tool name, status badge, and duration from timeline helpers. Wire terminal run outcomes (`run_complete`, `run_error`) to clear final UI state messaging and run-control re-enable behavior, while preserving one-active-run semantics established in prior plans. Keep presentation intentionally lean and do not add payload drill-down/history panels (reserved for Phase 5).</action>
  <verify>npm run typecheck && npm run test</verify>
  <done>User can run a prompt and observe real-time per-tool timeline updates plus stable final success/error state with no duplicated rows across reruns.</done>
</task>

</tasks>

<verification>
1. Start frontend/backend locally and run one prompt from the UI.
2. Confirm timeline shows each emitted `web_search`/`web_crawl` tool call with evolving status.
3. Confirm duration updates while running, then finalizes to backend `durationMs` when each call completes.
4. Confirm `run_complete`/`run_error` yields a stable final UI state and rerunning does not duplicate timeline rows.
</verification>

<success_criteria>
- OBS-01 user visibility is complete: timeline exposes per-tool status and duration during execution.
- Duration display honors backend truth (`durationMs`) for completed calls.
- UI behavior remains stable at terminal boundaries and across reruns.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-prompt-execution-surface/04-03-SUMMARY.md`
</output>
