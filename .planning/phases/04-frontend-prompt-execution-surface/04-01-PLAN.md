---
phase: 04-frontend-prompt-execution-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/contracts.ts
  - src/frontend/routes/runs.ts
  - src/frontend/server.ts
  - src/frontend/client/api-client.ts
  - src/frontend/client/state.ts
  - src/frontend/client/app.ts
  - public/index.html
  - src/tests/frontend-api/runs.contracts.test.ts
autonomous: true
must_haves:
  truths:
    - "User can enter a prompt in one frontend interface and start a run."
    - "Frontend sends the prompt to a backend run-start endpoint and receives a run identifier."
    - "UI transitions from idle to starting/running state after run creation."
  artifacts:
    - path: "public/index.html"
      provides: "Single prompt input and run trigger UI shell"
    - path: "src/frontend/routes/runs.ts"
      provides: "POST /api/runs endpoint wired into frontend server API router"
      exports: ["createRunsRouter"]
    - path: "src/frontend/contracts.ts"
      provides: "Zod-validated run start request/response contracts"
      contains: "RunStartRequestSchema and RunStartResponseSchema"
    - path: "src/frontend/client/api-client.ts"
      provides: "Typed client call for creating runs"
    - path: "src/frontend/client/state.ts"
      provides: "Run state reducer covering idle/starting/running transitions"
  key_links:
    - from: "public/index.html"
      to: "src/frontend/client/app.ts"
      via: "module script bootstraps prompt submit handler"
      pattern: "type=\"module\".*app"
    - from: "src/frontend/client/api-client.ts"
      to: "/api/runs"
      via: "POST JSON request"
      pattern: "fetch\\(\"/api/runs\""
    - from: "src/frontend/server.ts"
      to: "src/frontend/routes/runs.ts"
      via: "router.use(\"/runs\", createRunsRouter())"
      pattern: "createRunsRouter"
---

<objective>
Establish the minimal prompt execution surface for Phase 04 by adding a typed run-start API contract, backend route wiring, and a single-screen UI that can initiate one run.

Purpose: Satisfy RUNTIME-02 baseline so users can start a run from a minimal TypeScript frontend flow before adding live timeline streaming.
Output: A runnable prompt form + run button flow that calls `POST /api/runs`, parses validated response data, and updates run phase in frontend state.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-prompt-execution-surface/04-RESEARCH.md
@src/frontend/server.ts
@src/frontend/contracts.ts
@src/tests/frontend-api/routes.contracts.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed run-start contracts and backend route wiring</name>
  <files>src/frontend/contracts.ts, src/frontend/routes/runs.ts, src/frontend/server.ts, src/tests/frontend-api/runs.contracts.test.ts</files>
  <action>Extend frontend API contracts with Zod schemas and inferred types for run start (`POST /api/runs`) using request shape `{ prompt: string }` and response shape `{ runId: string, status: "queued" | "running" }`. Implement `createRunsRouter()` with strict payload parsing, error envelope behavior aligned with existing frontend route conventions, and deterministic status-code mapping for validation/runtime failures. Wire the new runs router under `/api/runs` in `server.ts` without changing existing `/api/search` and `/api/fetch` behavior. Add contract tests proving successful run start envelope parsing and malformed payload rejection; avoid introducing SSE handling in this task.</action>
  <verify>npm run test -- src/tests/frontend-api/runs.contracts.test.ts</verify>
  <done>`POST /api/runs` exists, validates request payloads, returns typed start response with `runId`, and contract tests pass for success + validation failure cases.</done>
</task>

<task type="auto">
  <name>Task 2: Build minimal one-screen prompt UI and run-start API client</name>
  <files>public/index.html, src/frontend/client/api-client.ts, src/frontend/client/state.ts, src/frontend/client/app.ts</files>
  <action>Create a minimal UI containing one prompt input, one run button, and a run status region. Implement a typed frontend API client that calls `POST /api/runs`, parses responses against run-start schemas, and returns normalized client results. Add a strict reducer for `idle -> starting -> running|failed` transitions, enforcing one-active-run behavior and rejecting invalid regressions. Wire submit handling so the run button triggers state transition to `starting`, invokes the API client, and updates state to `running` on success or `failed` on error. Keep rendering intentionally minimal and defer per-tool timeline rendering to 04-02.</action>
  <verify>npm run typecheck && npm run test -- src/tests/frontend-api/runs.contracts.test.ts</verify>
  <done>User can load the frontend, enter a prompt, start a run, and see run state transition from idle to starting/running or failed based on backend response.</done>
</task>

</tasks>

<verification>
1. Start frontend server with `npm run dev:frontend`.
2. Open local frontend page and submit a non-empty prompt.
3. Confirm network call to `POST /api/runs` succeeds and UI reflects starting/running state.
4. Submit an empty prompt and confirm a validation error path is surfaced without crashing UI state.
</verification>

<success_criteria>
- RUNTIME-02 is partially satisfied: one simple prompt/run frontend interface works end-to-end for run initiation.
- Backend and frontend remain schema-first and typed for run-start contracts.
- State handling is ready for SSE event integration in 04-02 without reworking run-start flow.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-prompt-execution-surface/04-01-SUMMARY.md`
</output>
