---
phase: "01-tool-contract-alignment"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/contracts/run-events.ts
  - src/frontend/contracts.ts
  - src/frontend/client/browser-contracts.ts
  - src/core/telemetry/observability-logger.ts
  - src/frontend/routes/fetch.ts
  - src/frontend/routes/runs.ts
  - src/frontend/client/state.ts
  - src/tests/frontend-api/run-events.contracts.test.ts
  - src/tests/frontend-api/observability-correlation.test.ts
autonomous: true
requirements: [TOOL-01, TOOL-02]
must_haves:
  truths:
    - "Frontend contract parsing accepts web_search and open_url as the only tool names."
    - "Frontend observability and run-history mapping still convert page-opening events into retrieval action type open_page."
    - "Fetch route and run-event reducers emit open_url consistently for new events."
  artifacts:
    - path: "src/frontend/contracts/run-events.ts"
      provides: "Canonical run-event tool-name schema"
      contains: 'z.enum(["web_search", "open_url"])'
    - path: "src/core/telemetry/observability-logger.ts"
      provides: "Tool-name to retrieval-action mapping"
      contains: 'if (input.toolName === "open_url")'
    - path: "src/frontend/routes/fetch.ts"
      provides: "Frontend fetch route observability emission"
      contains: 'toolName: "open_url"'
  key_links:
    - from: "src/frontend/routes/fetch.ts"
      to: "src/core/telemetry/observability-logger.ts"
      via: "toolName emission"
      pattern: 'toolName: "open_url"'
    - from: "src/frontend/routes/runs.ts"
      to: "src/frontend/client/state.ts"
      via: "tool call / progress event normalization"
      pattern: 'case "open_url"'
---

<objective>
Align frontend contracts, telemetry, and event normalization to the `open_url` tool name while preserving `open_page` observability semantics.

Purpose: The UI and event pipeline are exact-name sensitive, so Phase 1 is incomplete unless the browser/server side accepts and emits the renamed tool contract.
Output: Updated Zod/browser contracts, observability mapping, fetch-route emission, state normalization, and targeted frontend contract tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-tool-contract-alignment/01-RESEARCH.md
@.planning/phases/01-tool-contract-alignment/01-VALIDATION.md
@src/frontend/contracts/run-events.ts
@src/frontend/contracts.ts
@src/frontend/client/browser-contracts.ts
@src/core/telemetry/observability-logger.ts
@src/frontend/routes/fetch.ts
@src/frontend/routes/runs.ts
@src/frontend/client/state.ts
@src/tests/frontend-api/run-events.contracts.test.ts
@src/tests/frontend-api/observability-correlation.test.ts

<interfaces>
From src/frontend/contracts/run-events.ts:
```ts
export const RunEventToolNameSchema = z.enum(["web_search", "web_crawl"]);
```

From src/core/telemetry/observability-logger.ts:
```ts
if (input.toolName === "web_search") { ... }
if (input.toolName === "web_crawl") {
  return { action_type: "open_page", url };
}
```

From src/frontend/routes/fetch.ts:
```ts
emitToolCallStarted({ toolName: "web_crawl", ... });
emitToolCallSucceeded({ toolName: "web_crawl", ... });
emitToolCallFailed({ toolName: "web_crawl", ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update frontend schemas and browser parsers to make open_url canonical</name>
  <files>src/frontend/contracts/run-events.ts, src/frontend/contracts.ts, src/frontend/client/browser-contracts.ts, src/tests/frontend-api/run-events.contracts.test.ts</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/01-tool-contract-alignment/01-RESEARCH.md, src/frontend/contracts/run-events.ts, src/frontend/contracts.ts, src/frontend/client/browser-contracts.ts, src/tests/frontend-api/run-events.contracts.test.ts</read_first>
  <behavior>
    - Test 1: canonical frontend run-event schemas accept `tool_name: "open_url"`.
    - Test 2: browser contract parsing accepts `toolName: "open_url"` and rejects stale `web_crawl` where the canonical schema is exact-name only.
    - Test 3: retrieval action types remain `search`, `open_page`, and `find_in_page`.
  </behavior>
  <action>Per TOOL-01 and TOOL-02, update the exact-name frontend schemas so `web_search` remains unchanged and `open_url` replaces `web_crawl` anywhere tool names are part of the public run-event contract. Touch both `src/frontend/contracts/run-events.ts` and `src/frontend/contracts.ts`, then keep `src/frontend/client/browser-contracts.ts` in sync so browser parsers and TypeScript types use the same canonical values. Do not rename retrieval action enums or event-stage language; `open_page` remains the action concept. Extend `src/tests/frontend-api/run-events.contracts.test.ts` to assert canonical `open_url` parsing and to keep malformed-event rejection intact.</action>
  <verify>
    <automated>npm test -- src/tests/frontend-api/run-events.contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'RunEventToolNameSchema = z\\.enum\\(\\[\"web_search\", \"open_url\"\\]\\)' src/frontend/contracts/run-events.ts src/frontend/contracts.ts` returns matches.
    - `rg -n 'toolName: \"web_search\" \\| \"open_url\"|tool_name\\?: \"web_search\" \\| \"open_url\"' src/frontend/client/browser-contracts.ts` returns matches.
    - `rg -n 'action_type: \"search\" \\| \"open_page\" \\| \"find_in_page\"|actionType: \"open_page\"' src/frontend/contracts/run-events.ts src/frontend/contracts.ts src/frontend/client/browser-contracts.ts` returns matches.
    - `npm test -- src/tests/frontend-api/run-events.contracts.test.ts` exits 0.
  </acceptance_criteria>
  <done>Frontend canonical schemas and browser parsing treat `open_url` as the only page-opening tool name while leaving `open_page` semantics unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align telemetry and event normalization to emit open_url but still map to open_page</name>
  <files>src/core/telemetry/observability-logger.ts, src/frontend/routes/fetch.ts, src/frontend/routes/runs.ts, src/frontend/client/state.ts, src/tests/frontend-api/observability-correlation.test.ts</files>
  <read_first>.planning/phases/01-tool-contract-alignment/01-RESEARCH.md, .planning/phases/01-tool-contract-alignment/01-VALIDATION.md, src/core/telemetry/observability-logger.ts, src/frontend/routes/fetch.ts, src/frontend/routes/runs.ts, src/frontend/client/state.ts, src/tests/frontend-api/observability-correlation.test.ts</read_first>
  <behavior>
    - Test 1: fetch-route tool call events emit `toolName: "open_url"`.
    - Test 2: observability mapping still derives retrieval actions with `action_type: "open_page"`.
    - Test 3: run-state normalization paths treat `open_url` as the crawl/open tool everywhere `web_crawl` was previously matched.
  </behavior>
  <action>Update all emitting and normalizing frontend surfaces so new tool-call events use `open_url`. In `src/frontend/routes/fetch.ts`, replace every `toolName: "web_crawl"` emission with `toolName: "open_url"`. In `src/core/telemetry/observability-logger.ts`, make the page-opening branch key off `open_url` while continuing to derive `action_type: "open_page"`. In `src/frontend/routes/runs.ts` and `src/frontend/client/state.ts`, replace `case "web_crawl"` tool-name branches with `case "open_url"` while leaving progress messaging and retrieval-action logic unchanged. Extend `src/tests/frontend-api/observability-correlation.test.ts` to assert the new emitted tool name and unchanged `open_page` retrieval action.</action>
  <verify>
    <automated>npm test -- src/tests/frontend-api/observability-correlation.test.ts src/tests/frontend-api/run-events.contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'toolName: \"open_url\"' src/frontend/routes/fetch.ts` returns three matches for started, succeeded, and failed emission paths.
    - `rg -n 'input\\.toolName === \"open_url\"|case \"open_url\"' src/core/telemetry/observability-logger.ts src/frontend/routes/runs.ts src/frontend/client/state.ts` returns matches.
    - `rg -n 'action_type: \"open_page\"' src/core/telemetry/observability-logger.ts src/frontend/routes/runs.ts src/frontend/client/state.ts` returns matches.
    - `npm test -- src/tests/frontend-api/observability-correlation.test.ts src/tests/frontend-api/run-events.contracts.test.ts` exits 0.
  </acceptance_criteria>
  <done>Frontend emission and normalization paths produce `open_url` tool names end-to-end while preserving existing `open_page` action semantics and progress wording.</done>
</task>

</tasks>

<verification>
Run `npm test -- src/tests/frontend-api/run-events.contracts.test.ts src/tests/frontend-api/observability-correlation.test.ts` after the frontend work. The command must prove exact-name schema alignment plus `open_url` to `open_page` observability mapping.
</verification>

<success_criteria>
Frontend contract parsing, telemetry emission, and state normalization all use `open_url`; `web_search` remains unchanged; `open_page` continues to be the retrieval action emitted for page-opening work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tool-contract-alignment/01-tool-contract-alignment-02-SUMMARY.md`
</output>
