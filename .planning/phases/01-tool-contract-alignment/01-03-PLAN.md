---
phase: "01-tool-contract-alignment"
plan: "03"
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - backend/tests/api/test_agent_run_route.py
  - src/tests/frontend-api/run-history.integration.test.ts
  - src/tests/frontend-api/runs.stream.test.ts
  - src/tests/frontend/state.test.ts
  - src/tests/frontend/timeline.test.ts
autonomous: true
requirements: [TOOL-01, TOOL-02]
must_haves:
  truths:
    - "Queued deep-research HTTP requests keep their current response shape while the shared runtime contract uses open_url."
    - "Run history, SSE stream parsing, reducer state, and timeline helpers all surface open_url without breaking open_page traces."
    - "Phase 1 validation commands pass on the renamed contract."
  artifacts:
    - path: "backend/tests/api/test_agent_run_route.py"
      provides: "Queued deep-research regression coverage"
      contains: "deep_research"
    - path: "src/tests/frontend-api/run-history.integration.test.ts"
      provides: "Run-history contract coverage for open_url"
      contains: "open_url"
    - path: "src/tests/frontend/state.test.ts"
      provides: "Reducer/timeline regression coverage for open_url"
      contains: "open_url"
  key_links:
    - from: "backend/tests/api/test_agent_run_route.py"
      to: "backend/api/services/agent_run.py"
      via: "queued deep-research response contract"
      pattern: "status_code=202|execution_surface=\"background\""
    - from: "src/tests/frontend-api/run-history.integration.test.ts"
      to: "src/frontend/routes/runs.ts"
      via: "stored canonical run events"
      pattern: "tool_name|toolName"
---

<objective>
Lock the rename in with regression coverage across queued deep-research HTTP flow, SSE/history contracts, and frontend reducer/timeline surfaces.

Purpose: Phase 1 only ships safely if the unchanged queued deep-research API, shared runtime bindings, and UI event history all verify the renamed contract together.
Output: API and frontend integration tests updated to assert `open_url` end-to-end, with validation commands matching the phase validation strategy.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-tool-contract-alignment/01-RESEARCH.md
@.planning/phases/01-tool-contract-alignment/01-VALIDATION.md
@backend/api/services/agent_run.py
@backend/agent/deep_research_runtime.py
@backend/tests/api/test_agent_run_route.py
@src/frontend/routes/runs.ts
@src/tests/frontend-api/run-history.integration.test.ts
@src/tests/frontend-api/runs.stream.test.ts
@src/tests/frontend/state.test.ts
@src/tests/frontend/timeline.test.ts

<interfaces>
From backend/api/services/agent_run.py:
```python
if payload.mode == "deep_research":
    queued = start_deep_research_request(payload)
    return JSONResponse(status_code=202, content=queued.model_dump())
```

From src/frontend/routes/runs.ts:
```ts
switch (event.toolName) {
  case "web_search": ...
  case "web_crawl": ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add queued deep-research and shared-runtime regression coverage for the renamed tool contract</name>
  <files>backend/tests/api/test_agent_run_route.py</files>
  <read_first>.planning/phases/01-tool-contract-alignment/01-RESEARCH.md, .planning/phases/01-tool-contract-alignment/01-VALIDATION.md, backend/api/services/agent_run.py, backend/agent/deep_research_runtime.py, backend/tests/api/test_agent_run_route.py</read_first>
  <behavior>
    - Test 1: `mode="deep_research"` still returns the queued 202 response shape unchanged.
    - Test 2: regression coverage proves the rename did not require an HTTP envelope change to deep-research.
    - Test 3: API-side tests document that the runtime contract work is name-only, not behavior-altering.
  </behavior>
  <action>Extend `backend/tests/api/test_agent_run_route.py` so Phase 1 explicitly covers the unchanged queued deep-research HTTP response while documenting the renamed shared retrieval contract from the runtime layer. Add assertions around the 202 queued response, `execution_surface="background"`, and unchanged `AgentRunQueuedResponse` shape. Keep the test focused on contract stability; do not add new deep-research orchestration behavior here, because later phases own that work.</action>
  <verify>
    <automated>python -m pytest backend/tests/api/test_agent_run_route.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'deep_research|execution_surface=\"background\"|status_code=202' backend/tests/api/test_agent_run_route.py` returns matches.
    - `python -m pytest backend/tests/api/test_agent_run_route.py -q` exits 0.
  </acceptance_criteria>
  <done>The API regression suite proves the queued deep-research envelope is unchanged while Phase 1 tool-name alignment lands underneath it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add end-to-end UI event-history regressions for open_url while preserving open_page traces</name>
  <files>src/tests/frontend-api/run-history.integration.test.ts, src/tests/frontend-api/runs.stream.test.ts, src/tests/frontend/state.test.ts, src/tests/frontend/timeline.test.ts</files>
  <read_first>.planning/phases/01-tool-contract-alignment/01-VALIDATION.md, src/frontend/routes/runs.ts, src/frontend/client/state.ts, src/tests/frontend-api/run-history.integration.test.ts, src/tests/frontend-api/runs.stream.test.ts, src/tests/frontend/state.test.ts, src/tests/frontend/timeline.test.ts</read_first>
  <behavior>
    - Test 1: run-history snapshots and SSE stream fixtures use `open_url` as the tool name.
    - Test 2: reducer and timeline helpers still render page-opening work correctly after the rename.
    - Test 3: retrieval-action assertions continue to use `open_page`, not `open_url`.
  </behavior>
  <action>Update the frontend integration/regression suites to reflect the renamed tool contract wherever canonical run events, tool-call fixtures, reducer events, or timeline rows currently use `web_crawl`. Cover `src/tests/frontend-api/run-history.integration.test.ts`, `src/tests/frontend-api/runs.stream.test.ts`, `src/tests/frontend/state.test.ts`, and `src/tests/frontend/timeline.test.ts`. Keep every retrieval-action assertion on `open_page`, and preserve the existing event ordering, progress-stage, and retention expectations unless the rename itself requires a literal tool-name change.</action>
  <verify>
    <automated>npm test -- src/tests/frontend-api/run-history.integration.test.ts src/tests/frontend-api/runs.stream.test.ts src/tests/frontend/state.test.ts src/tests/frontend/timeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'open_url' src/tests/frontend-api/run-history.integration.test.ts src/tests/frontend-api/runs.stream.test.ts src/tests/frontend/state.test.ts src/tests/frontend/timeline.test.ts` returns matches.
    - `rg -n 'open_page' src/tests/frontend-api/run-history.integration.test.ts src/tests/frontend-api/runs.stream.test.ts src/tests/frontend/state.test.ts src/tests/frontend/timeline.test.ts` returns matches.
    - `npm test -- src/tests/frontend-api/run-history.integration.test.ts src/tests/frontend-api/runs.stream.test.ts src/tests/frontend/state.test.ts src/tests/frontend/timeline.test.ts` exits 0.
  </acceptance_criteria>
  <done>Frontend integration suites prove `open_url` flows through SSE, history, reducer, and timeline surfaces while `open_page` remains the stable action semantic.</done>
</task>

</tasks>

<verification>
Run the phase-gate commands from `01-VALIDATION.md` after this plan:
- `python -m pytest backend/tests/api/test_agent_run_route.py backend/tests/agent/test_runtime.py backend/tests/tools/test_web_crawl_tool.py -q`
- `npm test -- src/tests/frontend-api/run-events.contracts.test.ts src/tests/frontend-api/observability-correlation.test.ts src/tests/frontend-api/run-history.integration.test.ts src/tests/frontend-api/runs.stream.test.ts src/tests/frontend/state.test.ts src/tests/frontend/timeline.test.ts`
- `python -m pytest backend/tests -q && npm test -- --runInBand`
</verification>

<success_criteria>
The queued deep-research API still returns its existing envelope, and the combined backend/frontend regression suite confirms `web_search` + `open_url` across runtime, SSE/history, reducer state, and timeline surfaces without changing `open_page` semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tool-contract-alignment/01-tool-contract-alignment-03-SUMMARY.md`
</output>
