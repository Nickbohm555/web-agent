---
phase: "01-tool-contract-alignment"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/runtime_constants.py
  - backend/agent/runtime_execution.py
  - backend/agent/runtime_sources.py
  - backend/agent/prompts.py
  - backend/app/tools/__init__.py
  - backend/app/tools/web_crawl.py
  - backend/tests/agent/test_runtime.py
  - backend/tests/tools/test_web_crawl_tool.py
autonomous: true
requirements: [TOOL-01, TOOL-02]
must_haves:
  truths:
    - "Quick, agentic, and deep-research runtime bindings expose web_search as the search tool name."
    - "Quick, agentic, and deep-research runtime bindings expose open_url as the page-opening tool name."
    - "Backend source extraction still recognizes legacy web_crawl messages while new runs emit open_url."
  artifacts:
    - path: "backend/agent/runtime_constants.py"
      provides: "Canonical runtime tool-name tuple"
      contains: 'CANONICAL_TOOL_NAMES = ("web_search", "open_url")'
    - path: "backend/app/tools/web_crawl.py"
      provides: "User-facing page-opening LangChain tool"
      contains: '@tool("open_url", args_schema=WebCrawlInput)'
    - path: "backend/agent/runtime_sources.py"
      provides: "Backward-tolerant crawl/open_url parser logic"
      contains: '"open_url"'
  key_links:
    - from: "backend/agent/runtime_execution.py"
      to: "backend/app/tools/web_crawl.py"
      via: "canonical tool binding"
      pattern: "get_canonical_tools\\(\\).*open_url"
    - from: "backend/agent/runtime_sources.py"
      to: "backend/tests/agent/test_runtime.py"
      via: "legacy/new tool-name parsing coverage"
      pattern: "open_url|web_crawl"
---

<objective>
Make the backend runtime expose `open_url` as the canonical page-opening tool without changing crawl behavior.

Purpose: Phase 1 depends on a stable user-facing retrieval contract before later Deep Agents work starts depending on tool names.
Output: Canonical runtime bindings, prompts, parser tolerance, and backend regression coverage aligned to `web_search` + `open_url`.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tool-contract-alignment/01-RESEARCH.md
@.planning/phases/01-tool-contract-alignment/01-VALIDATION.md
@backend/agent/runtime_constants.py
@backend/agent/runtime_execution.py
@backend/agent/runtime_sources.py
@backend/agent/prompts.py
@backend/app/tools/web_crawl.py
@backend/tests/agent/test_runtime.py
@backend/tests/tools/test_web_crawl_tool.py

<interfaces>
From backend/agent/runtime_constants.py:
```python
CANONICAL_TOOL_NAMES = ("web_search", "web_crawl")
```

From backend/agent/runtime_execution.py:
```python
def get_canonical_tools() -> tuple[Any, Any]:
    return (web_search, web_crawl)

def get_tools_for_profile(...) -> tuple[Any, Any]:
    ...
```

From backend/app/tools/web_crawl.py:
```python
@tool("web_crawl", args_schema=WebCrawlInput)
def bounded_web_crawl(...) -> WebCrawlToolResult: ...

def run_web_crawl(...) -> WebCrawlToolResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename the canonical backend page-opening tool contract to open_url</name>
  <files>backend/agent/runtime_constants.py, backend/agent/runtime_execution.py, backend/agent/prompts.py, backend/app/tools/web_crawl.py, backend/app/tools/__init__.py</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/01-tool-contract-alignment/01-RESEARCH.md, backend/agent/runtime_constants.py, backend/agent/runtime_execution.py, backend/agent/prompts.py, backend/app/tools/web_crawl.py, backend/app/tools/__init__.py</read_first>
  <behavior>
    - Test 1: canonical backend tool names resolve to `("web_search", "open_url")`.
    - Test 2: the LangChain crawl/open tool instance reports `.name == "open_url"` while `run_web_crawl(...)` behavior stays unchanged.
    - Test 3: prompt guidance tells the agent to use `open_url`, not `web_crawl`.
  </behavior>
  <action>Per TOOL-01 and TOOL-02, change the backend’s user-facing page-opening contract from `web_crawl` to `open_url` while preserving the existing crawl implementation. Keep `run_web_crawl(...)`, `WebCrawl*` schemas, and retrieval action semantics untouched. In `backend/app/tools/web_crawl.py`, make the registered LangChain tool use `@tool("open_url", args_schema=WebCrawlInput)`, export a canonical `open_url` tool object, and keep a temporary `web_crawl = open_url` compatibility alias only for import stability during migration. In `backend/agent/runtime_execution.py` and `backend/agent/runtime_constants.py`, bind canonical tools as `web_search` + `open_url` for quick, agentic, and deep-research profiles. In `backend/agent/prompts.py`, replace user-facing prompt guidance strings so every instruction says `open_url` while retaining the same crawl-selection behavior and objective requirement. Update `backend/app/tools/__init__.py` exports so downstream imports can resolve `open_url` explicitly without removing `run_web_crawl`.</action>
  <verify>
    <automated>python -m pytest backend/tests/agent/test_runtime.py backend/tests/tools/test_web_crawl_tool.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'CANONICAL_TOOL_NAMES = \\("web_search", "open_url"\\)' backend/agent/runtime_constants.py` returns one match.
    - `rg -n '@tool\\("open_url"' backend/app/tools/web_crawl.py` returns one match.
    - `rg -n 'Use open_url|call open_url|When you call open_url' backend/agent/prompts.py` returns matches and `rg -n 'web_crawl' backend/agent/prompts.py` returns no matches in guidance text.
    - `rg -n '__all__ = .*open_url|if name in \\{"run_web_crawl", "web_crawl", "open_url"\\}' backend/app/tools/__init__.py` returns a match.
  </acceptance_criteria>
  <done>Backend canonical runtime binding emits `open_url` everywhere new tool instances are created, while crawl internals and typed payloads remain behavior-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make backend source extraction tolerate both tool names and lock it with regression tests</name>
  <files>backend/agent/runtime_sources.py, backend/tests/agent/test_runtime.py, backend/tests/tools/test_web_crawl_tool.py</files>
  <read_first>.planning/phases/01-tool-contract-alignment/01-RESEARCH.md, .planning/phases/01-tool-contract-alignment/01-VALIDATION.md, backend/agent/runtime_sources.py, backend/tests/agent/test_runtime.py, backend/tests/tools/test_web_crawl_tool.py</read_first>
  <behavior>
    - Test 1: `extract_sources(...)` and crawl-error extraction accept tool messages named `open_url`.
    - Test 2: the same parser paths still accept historical `web_crawl` tool messages.
    - Test 3: backend tests assert `open_page` action records remain unchanged.
  </behavior>
  <action>Per the research constraint about mixed-version tolerance, update `backend/agent/runtime_sources.py` so parser boundaries accept both `"web_crawl"` and `"open_url"` wherever tool names are read from runtime messages or payloads. Do not rename retrieval actions; keep `open_page` and `open_page_batch` exactly as they are. Extend `backend/tests/agent/test_runtime.py` to cover canonical tool-name assertions, prompt text, runtime tool construction for agentic and deep-research profiles, and source extraction from both legacy and new tool names. Extend `backend/tests/tools/test_web_crawl_tool.py` so the canonical tool instance reports `open_url`, compatibility imports still work, and action-record helpers still output `action_type: "open_page"` or `"open_page_batch"`.</action>
  <verify>
    <automated>python -m pytest backend/tests/agent/test_runtime.py backend/tests/tools/test_web_crawl_tool.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '"open_url"' backend/agent/runtime_sources.py` returns matches in the tool-name parsing paths.
    - `rg -n 'action_type\": \"open_page\"|action_type\": \"open_page_batch\"' backend/tests/tools/test_web_crawl_tool.py` returns matches.
    - `rg -n 'open_url' backend/tests/agent/test_runtime.py backend/tests/tools/test_web_crawl_tool.py` returns matches covering canonical binding and parser tolerance.
    - `python -m pytest backend/tests/agent/test_runtime.py backend/tests/tools/test_web_crawl_tool.py -q` exits 0.
  </acceptance_criteria>
  <done>Backend parsing, runtime assertions, and tests accept old `web_crawl` messages while treating `open_url` as the only new canonical contract.</done>
</task>

</tasks>

<verification>
Run `python -m pytest backend/tests/agent/test_runtime.py backend/tests/tools/test_web_crawl_tool.py -q` after each backend task. The combined run must prove canonical tool names are `web_search` + `open_url`, parser tolerance accepts both tool names, and retrieval action records still use `open_page`.
</verification>

<success_criteria>
Quick, agentic, and deep-research backend bindings all expose `open_url`; prompt text no longer instructs `web_crawl`; parser boundaries remain backward-tolerant; backend regression tests are green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tool-contract-alignment/01-tool-contract-alignment-01-SUMMARY.md`
</output>
