---
phase: 02-deep-agents-run-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - backend/api/schemas/agent_run.py
  - backend/api/schemas/__init__.py
  - backend/api/routes/agent_run.py
  - backend/api/services/agent_run.py
  - backend/agent/deep_research_lifecycle.py
  - backend/agent/deep_research_interrupts.py
  - backend/agent/deep_research_result_mapping.py
  - backend/agent/schemas/deep_research.py
  - backend/tests/agent/test_deep_research_runtime.py
  - backend/tests/agent/test_runtime.py
  - backend/tests/api/test_agent_run_service.py
  - backend/tests/api/test_agent_run_route.py
autonomous: true
requirements: [DEEP-02, DEEP-03]
must_haves:
  truths:
    - "User can receive no more than one clarification question at a time for a deep-research run."
    - "User can answer the clarification and continue the same deep-research thread instead of starting a new run."
    - "The HTTP deep-research response shape exposes a machine-readable clarification-pending state without breaking the existing top-level envelope."
  artifacts:
    - path: "backend/agent/deep_research_interrupts.py"
      provides: "Interrupt payload parsing and resume command helpers"
      min_lines: 20
    - path: "backend/agent/deep_research_lifecycle.py"
      provides: "Resume flow using the existing thread id and `Command(resume=...)`"
      min_lines: 60
    - path: "backend/api/schemas/agent_run.py"
      provides: "Request/response models for clarification-pending and resume input"
      min_lines: 80
    - path: "backend/api/services/agent_run.py"
      provides: "Service dispatch for start vs resume on the same deep-research run"
      min_lines: 30
  key_links:
    - from: "backend/agent/deep_research_lifecycle.py"
      to: "backend/agent/deep_research_interrupts.py"
      via: "interrupt parsing and resume payload mapping"
      pattern: "Command\\(resume="
    - from: "backend/api/services/agent_run.py"
      to: "backend/api/schemas/agent_run.py"
      via: "resume request fields such as `run_id` and `clarification_answer`"
      pattern: "clarification_answer|run_id"
    - from: "backend/api/routes/agent_run.py"
      to: "backend/api/services/agent_run.py"
      via: "same `/api/agent/run` route handling start and resume"
      pattern: "execute_agent_run_request"
---

<objective>
Add the Phase 2 clarification interrupt and same-thread resume lifecycle on top of the new Deep Agents start path.

Purpose: Satisfy DEEP-02 and DEEP-03 by enforcing one outstanding clarification at a time and resuming the same LangGraph thread with `Command(resume=...)`.
Output: Clarification-aware request/response schemas, interrupt/resume lifecycle helpers, updated API service routing, and tests proving same-thread continuation.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deep-agents-run-lifecycle/02-RESEARCH.md
@.planning/phases/02-deep-agents-run-lifecycle/02-VALIDATION.md
@.planning/phases/02-deep-agents-run-lifecycle/02-01-PLAN.md
@backend/api/schemas/agent_run.py
@backend/api/routes/agent_run.py
@backend/api/services/agent_run.py
@backend/agent/deep_research_lifecycle.py
@backend/agent/deep_research_result_mapping.py
@backend/agent/schemas/deep_research.py
@backend/tests/agent/test_deep_research_runtime.py
@backend/tests/api/test_agent_run_service.py
@backend/tests/api/test_agent_run_route.py

<interfaces>
From Phase 02-01 expected output:
```python
def start_deep_research_run(
    *,
    prompt: str,
    retrieval_policy: AgentRunRetrievalPolicy,
) -> AgentRunQueuedResponse: ...
```

Required resume pattern from research:
```python
from langgraph.types import Command

agent.invoke(Command(resume=clarification_answer), config=config)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the deep-research API contract for clarification-pending and resume input</name>
  <read_first>
    - backend/api/schemas/agent_run.py
    - backend/api/routes/agent_run.py
    - backend/agent/schemas/deep_research.py
    - .planning/phases/02-deep-agents-run-lifecycle/02-RESEARCH.md
  </read_first>
  <files>backend/api/schemas/agent_run.py, backend/api/schemas/__init__.py, backend/agent/schemas/deep_research.py, backend/tests/api/test_agent_run_route.py, backend/tests/api/test_agent_run_service.py</files>
  <action>Define the explicit Phase 2 clarification contract per D-02 and D-03 from research: extend `AgentRunRequest` with optional deep-research resume fields `run_id` and `clarification_answer`; add or expand the queued-style response metadata so a pending clarification can be represented with exact machine-readable fields `status=\"needs_clarification\"`, `metadata.execution_surface=\"background\"`, `metadata.thread_id`, and `metadata.clarification_question`; update `backend/agent/schemas/deep_research.py` with fields that record whether a clarification is outstanding and what question is pending; export the new schema types through `backend/api/schemas/__init__.py`; update service/route tests first so they lock the contract before lifecycle wiring. Keep the top-level envelope stable: `run_id`, `status`, and `metadata` remain the outer shape.</action>
  <acceptance_criteria>
    - `rg -n "needs_clarification|clarification_question|clarification_answer|thread_id" backend/api/schemas/agent_run.py backend/agent/schemas/deep_research.py` finds the new contract fields.
    - `rg -n "run_id: str|clarification_answer" backend/api/schemas/agent_run.py` shows resume input is explicitly typed, not raw dict access.
    - `pytest backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q` passes with assertions covering the clarification-pending response shape.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q</automated>
  </verify>
  <done>The API contract can represent one pending clarification and carry the identifiers needed to resume the same deep-research thread.</done>
</task>

<task type="auto">
  <name>Task 2: Implement single-question interrupt handling and same-thread resume</name>
  <read_first>
    - backend/agent/deep_research_lifecycle.py
    - backend/api/services/agent_run.py
    - backend/agent/deep_research_result_mapping.py
    - .planning/phases/02-deep-agents-run-lifecycle/02-RESEARCH.md
  </read_first>
  <files>backend/agent/deep_research_interrupts.py, backend/agent/deep_research_lifecycle.py, backend/agent/deep_research_result_mapping.py, backend/api/services/agent_run.py, backend/api/routes/agent_run.py, backend/tests/agent/test_deep_research_runtime.py, backend/tests/agent/test_runtime.py, backend/tests/api/test_agent_run_service.py, backend/tests/api/test_agent_run_route.py</files>
  <action>Implement the clarification lifecycle per D-02 and D-03: add `backend/agent/deep_research_interrupts.py` to normalize the Deep Agents interrupt payload into one outstanding clarification question and to build `Command(resume=clarification_answer)` for resume; update `backend/agent/deep_research_lifecycle.py` so start requests can surface a clarification interrupt as `status=\"needs_clarification\"` and resume requests reuse the stored `thread_id` instead of rebuilding the run; update `backend/agent/deep_research_result_mapping.py` so it preserves `run_id` and `thread_id` across start/resume mappings; update `backend/api/services/agent_run.py` to choose between `start_deep_research_run(...)` and `resume_deep_research_run(...)` based on `payload.run_id` plus `payload.clarification_answer`; keep `backend/api/routes/agent_run.py` on the existing `/api/agent/run` endpoint, only widening its response model as needed. Enforce exactly one outstanding clarification question at a time; do not allow arrays, batched questions, or a fallback that starts a new thread on resume.</action>
  <acceptance_criteria>
    - `rg -n "Command\\(resume=" backend/agent/deep_research_interrupts.py backend/agent/deep_research_lifecycle.py` shows same-thread resume uses the LangGraph primitive.
    - `rg -n "needs_clarification" backend/agent/deep_research_lifecycle.py backend/agent/deep_research_result_mapping.py backend/api/services/agent_run.py` shows the pending-clarification state is propagated end to end.
    - `rg -n "resume_deep_research_run|start_deep_research_run" backend/api/services/agent_run.py` shows explicit direct dispatch, not generic runner indirection.
    - `pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/agent/test_runtime.py backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/agent/test_runtime.py backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q</automated>
  </verify>
  <done>Deep-research runs can pause for one clarification question and resume the same thread with `Command(resume=...)` through the existing API route.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full Phase 2 validation slice for start, clarification, and resume</name>
  <read_first>
    - .planning/phases/02-deep-agents-run-lifecycle/02-VALIDATION.md
    - backend/tests/agent/test_deep_research_runtime.py
    - backend/tests/agent/test_runtime.py
    - backend/tests/api/test_agent_run_service.py
    - backend/tests/api/test_agent_run_route.py
  </read_first>
  <files>backend/tests/agent/test_deep_research_runtime.py, backend/tests/agent/test_runtime.py, backend/tests/api/test_agent_run_service.py, backend/tests/api/test_agent_run_route.py</files>
  <action>Run the Phase 2 full validation command from `02-VALIDATION.md`, fix any mismatches in clarification or resume behavior that the suite exposes, and ensure the resulting assertions explicitly prove: one clarification at a time, same-thread continuation, and no regression to the old queued placeholder lifecycle.</action>
  <acceptance_criteria>
    - `pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/agent/test_runtime.py backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q` exits 0.
    - `rg -n "needs_clarification|Command\\(resume=|thread_id" backend/tests/agent/test_deep_research_runtime.py backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py` finds assertions covering the clarification and resume contract.
    - `rg -n "queued wave|schedule_job|run_deep_research_job" backend/tests/agent/test_deep_research_runtime.py` does not show the old placeholder lifecycle as the asserted primary behavior.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/agent/test_runtime.py backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q</automated>
  </verify>
  <done>Phase 2’s clarification and resume lifecycle is green under the full validation slice and ready for persistence work in Phase 3.</done>
</task>

</tasks>

<verification>
Run `pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/agent/test_runtime.py backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q` and confirm the assertions cover `needs_clarification`, one-question-at-a-time semantics, and same-thread resume via `Command(resume=...)`.
</verification>

<success_criteria>
DEEP-02 and DEEP-03 are satisfied: the backend can surface one clarification question at a time, accept the answer through the existing API contract, and continue the same Deep Agents thread instead of creating a new run.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deep-agents-run-lifecycle/02-02-SUMMARY.md`
</output>
