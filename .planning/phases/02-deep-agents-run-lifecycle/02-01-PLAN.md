---
phase: 02-deep-agents-run-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/agent/deep_research_runtime.py
  - backend/agent/deep_research_agent.py
  - backend/agent/deep_research_lifecycle.py
  - backend/agent/deep_research_threading.py
  - backend/agent/deep_research_result_mapping.py
  - backend/agent/schemas/deep_research.py
  - backend/api/services/agent_run.py
  - backend/tests/agent/test_deep_research_runtime.py
  - backend/tests/api/test_agent_run_service.py
  - backend/tests/api/test_agent_run_route.py
autonomous: true
requirements: [DEEP-01]
must_haves:
  truths:
    - "User can start a `deep_research` run and the backend routes it into a Deep Agents supervisor built with `create_deep_agent(...)`."
    - "The initial deep-research response returns a stable run/thread identifier that later phases can resume."
    - "The HTTP deep-research entry path no longer depends on the placeholder queued wave executor as the primary runtime."
  artifacts:
    - path: "backend/agent/deep_research_agent.py"
      provides: "Concrete `create_deep_agent(...)` factory for the deep-research supervisor"
      min_lines: 20
    - path: "backend/agent/deep_research_lifecycle.py"
      provides: "Start-run orchestration that invokes the Deep Agents supervisor on a stable thread id"
      min_lines: 40
    - path: "backend/agent/deep_research_threading.py"
      provides: "Reusable thread/config helpers for start and later resume flows"
      min_lines: 10
    - path: "backend/api/services/agent_run.py"
      provides: "HTTP service dispatch into the new deep-research lifecycle"
      min_lines: 20
  key_links:
    - from: "backend/api/services/agent_run.py"
      to: "backend/agent/deep_research_lifecycle.py"
      via: "direct `start_deep_research_run(...)` call for `mode == \"deep_research\"`"
      pattern: "start_deep_research_run"
    - from: "backend/agent/deep_research_lifecycle.py"
      to: "backend/agent/deep_research_agent.py"
      via: "concrete supervisor construction"
      pattern: "create_deep_agent"
    - from: "backend/agent/deep_research_lifecycle.py"
      to: "backend/agent/deep_research_threading.py"
      via: "thread/config creation"
      pattern: "thread_id"
---

<objective>
Replace the Phase 2 deep-research entry path with a Deep Agents supervisor start lifecycle instead of the placeholder queued runtime.

Purpose: Satisfy DEEP-01 with a doc-aligned `create_deep_agent(...)` start path that later clarification and persistence work can build on.
Output: New Deep Agents lifecycle modules, updated deep-research schemas/service wiring, and tests proving start requests route through the new supervisor path.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deep-agents-run-lifecycle/02-RESEARCH.md
@.planning/phases/02-deep-agents-run-lifecycle/02-VALIDATION.md
@backend/api/services/agent_run.py
@backend/agent/deep_research_runtime.py
@backend/agent/runtime_execution.py
@backend/agent/schemas/deep_research.py
@backend/api/schemas/agent_run.py
@backend/tests/agent/test_deep_research_runtime.py
@backend/tests/api/test_agent_run_service.py
@backend/tests/api/test_agent_run_route.py

<interfaces>
From backend/api/services/agent_run.py:
```python
def execute_agent_run_request(
    payload: AgentRunRequest,
) -> AgentRunSuccessResponse | JSONResponse: ...
```

From backend/api/schemas/agent_run.py:
```python
class AgentRunQueuedResponse(BaseModel):
    run_id: str
    status: Literal["queued", "running"]
    metadata: AgentRunQueuedMetadata
```

From backend/agent/deep_research_runtime.py:
```python
def start_deep_research(
    *,
    prompt: str,
    retrieval_policy: AgentRunRetrievalPolicy,
    store: InMemoryDeepResearchStore | None = None,
    schedule_job: Callable[[str], None] | None = None,
    run_id_factory: Callable[[], str] | None = None,
) -> AgentRunQueuedResponse: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish Deep Agents lifecycle contracts and package seams</name>
  <read_first>
    - backend/requirements.txt
    - backend/agent/deep_research_runtime.py
    - backend/agent/schemas/deep_research.py
    - .planning/phases/02-deep-agents-run-lifecycle/02-RESEARCH.md
  </read_first>
  <files>backend/requirements.txt, backend/agent/schemas/deep_research.py, backend/agent/deep_research_threading.py, backend/agent/deep_research_result_mapping.py, backend/tests/agent/test_deep_research_runtime.py</files>
  <action>Create the Phase 2 contract layer per D-01 and D-04 from research: add the compatible `deepagents` dependency to `backend/requirements.txt`; expand `backend/agent/schemas/deep_research.py` with explicit lifecycle fields for a stable `thread_id`, doc-aligned run state, and the minimal supervisor outcome data needed by Phase 2 start/resume; add `backend/agent/deep_research_threading.py` with direct helpers that build the LangGraph config `{\"configurable\": {\"thread_id\": ...}}`; add `backend/agent/deep_research_result_mapping.py` to map Deep Agents start outcomes into the existing API envelope without raw dict payloads; update `backend/tests/agent/test_deep_research_runtime.py` first so it asserts the new thread-aware lifecycle contract before implementation. Do not add Postgres or durable backend plumbing here; keep that seam minimal for Phase 3.</action>
  <acceptance_criteria>
    - `rg -n "deepagents" backend/requirements.txt` returns one dependency line.
    - `rg -n "thread_id" backend/agent/schemas/deep_research.py backend/agent/deep_research_threading.py` finds the stable thread field and config helper.
    - `rg -n "def .*result.*mapping|class .*Metadata" backend/agent/deep_research_result_mapping.py` finds typed mapping logic instead of raw dict assembly.
    - `pytest backend/tests/agent/test_deep_research_runtime.py -q` passes with assertions that mention the thread-aware lifecycle fields.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/agent/test_deep_research_runtime.py -q</automated>
  </verify>
  <done>Phase 2 has explicit Deep Agents lifecycle schemas, a stable thread/config seam, and failing-or-passing tests that define the new start contract before the supervisor wiring is added.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder deep-research start path with a Deep Agents supervisor start flow</name>
  <read_first>
    - backend/api/services/agent_run.py
    - backend/agent/deep_research_runtime.py
    - backend/agent/runtime_execution.py
    - backend/api/schemas/agent_run.py
    - .planning/phases/02-deep-agents-run-lifecycle/02-RESEARCH.md
  </read_first>
  <files>backend/agent/deep_research_agent.py, backend/agent/deep_research_lifecycle.py, backend/agent/deep_research_runtime.py, backend/api/services/agent_run.py, backend/tests/api/test_agent_run_service.py, backend/tests/api/test_agent_run_route.py</files>
  <action>Implement the concrete Deep Agents start path per D-01 and D-04: create `backend/agent/deep_research_agent.py` with a direct `create_deep_research_agent(...)` wrapper around `create_deep_agent(...)`; create `backend/agent/deep_research_lifecycle.py` with `start_deep_research_run(...)` that builds the supervisor, invokes it with the stable `thread_id`, and maps the initial outcome through `backend/agent/deep_research_result_mapping.py`; update `backend/api/services/agent_run.py` so `mode == "deep_research"` calls `start_deep_research_run(...)` instead of the placeholder `start_deep_research(...)` queue flow; reduce `backend/agent/deep_research_runtime.py` to a compatibility wrapper or remove queued orchestration logic while preserving direct call paths; expand service and route tests so they prove the HTTP layer now returns the Deep Agents-backed response shape and never falls through to `run_agent_once(...)` for deep research. Keep the API envelope compatible with the current queued/success structure for this plan; clarification-specific response changes belong in 02-02.</action>
  <acceptance_criteria>
    - `rg -n "create_deep_agent" backend/agent/deep_research_agent.py backend/agent/deep_research_lifecycle.py` shows the new supervisor factory and lifecycle using the official API.
    - `rg -n "start_deep_research_run" backend/api/services/agent_run.py backend/agent/deep_research_lifecycle.py` shows direct service-to-lifecycle wiring.
    - `rg -n "start_deep_research\\(" backend/api/services/agent_run.py` returns no matches, proving the placeholder queue entry is no longer the primary path.
    - `pytest backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q</automated>
  </verify>
  <done>Deep-research start requests are handled by a `create_deep_agent(...)` supervisor lifecycle with a stable thread id and verified HTTP/service coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase quick validation slice for the Deep Agents start path</name>
  <read_first>
    - .planning/phases/02-deep-agents-run-lifecycle/02-VALIDATION.md
    - backend/tests/agent/test_deep_research_runtime.py
    - backend/tests/api/test_agent_run_service.py
  </read_first>
  <files>backend/tests/agent/test_deep_research_runtime.py, backend/tests/api/test_agent_run_service.py, backend/tests/api/test_agent_run_route.py</files>
  <action>Run the Wave 1 validation slice exactly as specified in `02-VALIDATION.md`: use the deep-research lifecycle unit tests plus the service/route tests touched above, fix any contract mismatches discovered by the quick validation run, and leave the test suite ready for Plan 02 to add clarification/resume behavior on top of the new start path.</action>
  <acceptance_criteria>
    - `pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/api/test_agent_run_service.py -q` exits 0.
    - `pytest backend/tests/api/test_agent_run_route.py -q` exits 0.
    - `rg -n "create_deep_agent|thread_id" backend/tests/agent/test_deep_research_runtime.py backend/tests/api/test_agent_run_service.py` finds assertions covering the new Deep Agents start path.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q</automated>
  </verify>
  <done>The Wave 1 lifecycle start contract is green under the phase validation slice and is ready for clarification/resume work.</done>
</task>

</tasks>

<verification>
Run `pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q` and confirm the deep-research path is exercised through the new lifecycle modules rather than the queued job runner.
</verification>

<success_criteria>
DEEP-01 is satisfied for the backend start path: `deep_research` requests create a Deep Agents supervisor with `create_deep_agent(...)`, carry a stable thread id, and return a validated queued/running-style response through the existing HTTP service boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deep-agents-run-lifecycle/02-01-SUMMARY.md`
</output>
