---
phase: 04-deep-agents-research-fan-out
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/agent/deep_research_artifacts.py
  - backend/agent/deep_research_progress.py
  - backend/agent/deep_research_execution.py
  - backend/agent/deep_research_runtime.py
  - backend/tests/agent/test_deep_research_runtime.py
autonomous: true
requirements:
  - RSCH-01
  - RSCH-02
must_haves:
  truths:
    - User can have plan-derived subquestions executed through a runtime path that records progress for each delegated research thread.
    - User can have each research-thread result persisted as a concise subanswer plus normalized sources and citations.
    - User can inspect progress and artifact paths for delegated subquestions through saved job state and runtime logs or artifacts.
  artifacts:
    - path: backend/agent/deep_research_execution.py
      provides: Fan-out executor that delegates subquestions, validates subagent outputs, and aggregates artifacts
      min_lines: 60
    - path: backend/agent/deep_research_artifacts.py
      provides: Artifact-path builder and per-subquestion artifact records
      min_lines: 30
    - path: backend/agent/deep_research_progress.py
      provides: Progress event records keyed by subquestion or subagent name
      min_lines: 30
    - path: backend/agent/deep_research_runtime.py
      provides: Runtime wiring from persisted plan to fan-out execution and saved job state
      min_lines: 60
    - path: backend/tests/agent/test_deep_research_runtime.py
      provides: Runtime coverage for progress, artifact persistence, and evidence aggregation
      min_lines: 80
  key_links:
    - from: backend/agent/deep_research_runtime.py
      to: backend/agent/deep_research_execution.py
      via: wave_executor parameter and direct call
      pattern: execute_research_waves
    - from: backend/agent/deep_research_execution.py
      to: backend/agent/deep_research_supervisor.py
      via: build_deep_research_supervisor
      pattern: build_deep_research_supervisor
    - from: backend/agent/deep_research_execution.py
      to: backend/agent/runtime_sources.py
      via: normalized source and citation extraction
      pattern: RuntimeSourceRegistry|extract_final_answer
    - from: backend/agent/deep_research_execution.py
      to: backend/agent/deep_research_artifacts.py
      via: artifact record write
      pattern: artifact_path
---

<objective>
Wire the Deep Agents research-subagent foundation into the current deep-research runtime so plan-derived subquestions execute through a real fan-out path, aggregate normalized evidence, and persist inspectable progress.

Purpose: Convert the current placeholder wave executor into a runtime path that actually delegates, records, and saves subquestion work.
Output: Runtime, execution, artifact, and progress modules plus updated tests proving persisted fan-out behavior.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deep-agents-research-fan-out/04-RESEARCH.md
@.planning/phases/04-deep-agents-research-fan-out/04-VALIDATION.md
@.planning/phases/04-deep-agents-research-fan-out/04-01-PLAN.md
@backend/agent/deep_research_runtime.py
@backend/agent/deep_research_execution.py
@backend/agent/runtime_sources.py
@backend/agent/schemas/deep_research.py
@backend/tests/agent/test_deep_research_runtime.py

<interfaces>
From backend/agent/schemas/deep_research.py:
```python
class DeepResearchJob(BaseModel):
    job_id: str
    prompt: str
    retrieval_policy: AgentRunRetrievalPolicy
    stage: DeepResearchStage
    sub_questions: list[str] = Field(default_factory=list)
    sources: list[AgentSourceReference] = Field(default_factory=list)
    wave_count: int = Field(default=0, ge=0)
    final_answer: Optional[AgentStructuredAnswer] = None
    error: Optional[AgentRunError] = None
```

From backend/agent/runtime_sources.py:
```python
class RuntimeSourceRegistry:
    @classmethod
    def empty(cls) -> "RuntimeSourceRegistry": ...
    def register(self, *, url: str, title: str, snippet: str = "", alias_urls: tuple[str, ...] = ()) -> None: ...
    def source_lookup(self) -> dict[str, AgentSourceReference]: ...

def extract_final_answer(raw_result: Any, source_lookup: dict[str, AgentSourceReference] | None = None) -> AgentStructuredAnswer: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend runtime tests for fan-out progress, artifact paths, and normalized evidence</name>
  <files>backend/tests/agent/test_deep_research_runtime.py</files>
  <read_first>
    - backend/tests/agent/test_deep_research_runtime.py
    - backend/tests/agent/test_deep_research_subagents.py
    - backend/agent/deep_research_runtime.py
    - backend/agent/deep_research_execution.py
    - backend/agent/schemas/deep_research.py
  </read_first>
  <behavior>
    - Test 1: the runtime saves `wave_count` equal to the number of delegated subquestions and keeps `stage == "completed"` after successful fan-out.
    - Test 2: each delegated subquestion produces an artifact path under `/workspace/research/`.
    - Test 3: aggregated job sources contain normalized source references from delegated subagent outputs.
  </behavior>
  <action>Update `backend/tests/agent/test_deep_research_runtime.py` so the main integration case stops using a trivial `wave_executor` stub and instead asserts runtime outputs from the real `execute_research_waves(...)` path through injected seams. Add explicit assertions for an artifact path prefix `/workspace/research/`, for `wave_count == 2` when two subquestions are planned, and for a normalized source URL `https://example.com/source`. Add focused `-k progress` and `-k evidence` cases matching the validation file names so `pytest backend/tests/agent/test_deep_research_runtime.py -q -k progress` and `... -k evidence` both run real tests.</action>
  <acceptance_criteria>
    - `rg -n "/workspace/research/" backend/tests/agent/test_deep_research_runtime.py` returns at least 1 match.
    - `rg -n "wave_count == 2" backend/tests/agent/test_deep_research_runtime.py` returns at least 1 match.
    - `rg -n "https://example.com/source" backend/tests/agent/test_deep_research_runtime.py` returns at least 1 match.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/agent/test_deep_research_runtime.py -q -k "progress or evidence"</automated>
  </verify>
  <done>The runtime test module proves the Phase 4 execution path records fan-out progress, artifact paths, and normalized evidence for delegated subquestions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add artifact/progress modules and replace the placeholder wave executor with Deep Agents fan-out wiring</name>
  <files>backend/agent/deep_research_artifacts.py, backend/agent/deep_research_progress.py, backend/agent/deep_research_execution.py, backend/agent/deep_research_runtime.py</files>
  <read_first>
    - backend/agent/deep_research_runtime.py
    - backend/agent/deep_research_execution.py
    - backend/agent/deep_research_supervisor.py
    - backend/agent/deep_research_subagents.py
    - backend/agent/runtime_sources.py
    - backend/agent/schemas/deep_research.py
    - backend/tests/agent/test_deep_research_runtime.py
  </read_first>
  <behavior>
    - Test 1: `execute_research_waves(...)` delegates each subquestion through a supervisor-created task path and aggregates subagent results.
    - Test 2: the runtime saves inspectable progress records and artifact paths on the job before final synthesis.
  </behavior>
  <action>Create `backend/agent/deep_research_artifacts.py` with direct helpers `build_research_artifact_path(job_id: str, subquestion_index: int, slug: str) -> str` returning `/workspace/research/{job_id}/{subquestion_index:02d}-{slug}.md` and `build_research_artifact_record(...)` returning a typed dict or Pydantic model from Plan 01. Create `backend/agent/deep_research_progress.py` with direct helpers `build_subquestion_progress_record(subquestion: str, status: str, artifact_path: str | None = None) -> dict[str, str | None]` and `append_progress_event(events: list[dict[str, str | None]], ...) -> list[dict[str, str | None]]`. Replace the placeholder body of `execute_research_waves(job)` in `backend/agent/deep_research_execution.py` with concrete logic that: builds a supervisor from `build_deep_research_supervisor(retrieval_policy=job.retrieval_policy)`, iterates each `job.sub_questions`, delegates each subquestion through an injected callable seam such as `delegate_subquestion(supervisor, subquestion)`, validates each result against `DeepResearchSubagentResult`, registers every returned source in `RuntimeSourceRegistry.empty()`, updates `wave_count=len(job.sub_questions)`, and returns a copied job with `stage=DeepResearchStage.SEARCHING`, aggregated `sources`, and progress or artifact metadata persisted through the new helpers. Update `backend/agent/deep_research_runtime.py` so `run_deep_research_job(...)` preserves the artifact/progress fields coming back from `execute_research_waves(...)` and still moves through `PLANNING`, `SEARCHING`, `VERIFYING`, `SYNTHESIZING`, and `COMPLETED`/`FAILED`. Keep the runtime entrypoint direct; do not add a generic orchestration runner.</action>
  <acceptance_criteria>
    - `rg -n "/workspace/research/\\{job_id\\}/\\{subquestion_index:02d\\}-\\{slug\\}\\.md" backend/agent/deep_research_artifacts.py` returns 1 match.
    - `rg -n "build_deep_research_supervisor" backend/agent/deep_research_execution.py` returns at least 1 match.
    - `rg -n "wave_count=len\\(job.sub_questions\\)" backend/agent/deep_research_execution.py` returns at least 1 match.
    - `rg -n "RuntimeSourceRegistry.empty\\(\\)" backend/agent/deep_research_execution.py` returns at least 1 match.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/agent/test_deep_research_runtime.py -q</automated>
  </verify>
  <done>The deep-research runtime now uses a real fan-out execution path that records per-subquestion progress and artifacts, validates delegated outputs, and saves normalized evidence onto the job.</done>
</task>

</tasks>

<verification>
Run `pytest backend/tests/agent/test_deep_research_subagents.py -q` and `pytest backend/tests/agent/test_deep_research_runtime.py -q` after this plan. The runtime test suite must show delegated subquestion progress and evidence aggregation; the subagent suite must continue proving retrieval-tool injection and supervisor construction.
</verification>

<success_criteria>
The Phase 4 runtime no longer uses a placeholder wave executor. Plan-derived subquestions are delegated through the Deep Agents fan-out path, each delegated result carries reusable evidence, and progress is inspectable through saved artifact or status records.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deep-agents-research-fan-out/04-02-SUMMARY.md`
</output>
