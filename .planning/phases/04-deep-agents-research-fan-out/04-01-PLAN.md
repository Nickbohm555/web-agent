---
phase: 04-deep-agents-research-fan-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/agent/test_deep_research_subagents.py
  - backend/agent/schemas/deep_research_subagent.py
  - backend/agent/deep_research_retrieval_tools.py
  - backend/agent/deep_research_subagents.py
  - backend/agent/deep_research_supervisor.py
autonomous: true
requirements:
  - RSCH-01
  - RSCH-02
must_haves:
  truths:
    - User can start a deep-research run that delegates multiple plan-derived subquestions through parallel Deep Agents subagents.
    - User can have every research subagent call the same backend retrieval behavior already exposed as web search and URL opening.
    - User can inspect one persisted artifact target per subquestion so fan-out work is not opaque.
  artifacts:
    - path: backend/agent/schemas/deep_research_subagent.py
      provides: Typed Pydantic models for subagent assignments, normalized evidence, and artifact references
      min_lines: 40
    - path: backend/agent/deep_research_retrieval_tools.py
      provides: Shared retrieval-tool builders that expose the canonical Deep Agents retrieval surface
      min_lines: 40
    - path: backend/agent/deep_research_subagents.py
      provides: Research subagent spec builder with explicit tool injection
      min_lines: 40
    - path: backend/agent/deep_research_supervisor.py
      provides: Supervisor factory using create_deep_agent(...) and task-style delegation
      min_lines: 40
    - path: backend/tests/agent/test_deep_research_subagents.py
      provides: Fan-out and retrieval reuse coverage for RSCH-01 and RSCH-02
      min_lines: 80
  key_links:
    - from: backend/agent/deep_research_supervisor.py
      to: backend/agent/deep_research_subagents.py
      via: create_deep_agent(..., subagents=[...])
      pattern: create_deep_agent
    - from: backend/agent/deep_research_subagents.py
      to: backend/agent/deep_research_retrieval_tools.py
      via: injected tools list
      pattern: tools
    - from: backend/agent/deep_research_retrieval_tools.py
      to: backend/app/tools/web_search.py
      via: build_web_search_tool import
      pattern: build_web_search_tool
    - from: backend/agent/deep_research_retrieval_tools.py
      to: backend/app/tools/web_crawl.py
      via: build_web_crawl_tool or open_url alias
      pattern: build_web_crawl_tool|open_url
---

<objective>
Create the Phase 4 fan-out foundation: typed Deep Agents research-subagent contracts, explicit retrieval-tool injection, and a supervisor factory that delegates plan-derived subquestions through task-style subagents.

Purpose: Establish the concrete delegation and retrieval interfaces before runtime wiring depends on them.
Output: New subagent schema/tool/supervisor modules and a dedicated pytest file that proves fan-out and retrieval reuse.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deep-agents-research-fan-out/04-RESEARCH.md
@.planning/phases/04-deep-agents-research-fan-out/04-VALIDATION.md
@backend/agent/deep_research_runtime.py
@backend/agent/deep_research_planning.py
@backend/agent/deep_research_execution.py
@backend/agent/runtime_execution.py
@backend/agent/runtime_sources.py
@backend/app/tools/web_search.py
@backend/app/tools/web_crawl.py

<interfaces>
From backend/app/tools/web_search.py:
```python
def build_web_search_tool(
    *,
    max_results_cap: int = 5,
    retrieval_policy: AgentRunRetrievalPolicy | None = None,
    search_runner: Callable[..., WebSearchToolResult] | None = None,
)
```

From backend/app/tools/web_crawl.py:
```python
def build_web_crawl_tool(
    *,
    max_content_chars: int = 6000,
    retrieval_policy: AgentRunRetrievalPolicy | None = None,
    fetch_worker: HttpFetchWorker | None = None,
    session_profile_provider: SessionProfileProvider | None = None,
    browser_fetcher=None,
)
```

From backend/agent/runtime_sources.py:
```python
class RuntimeSourceRegistry:
    def register(self, *, url: str, title: str, snippet: str = "", alias_urls: tuple[str, ...] = ()) -> None: ...

def extract_final_answer(raw_result: Any, source_lookup: dict[str, AgentSourceReference] | None = None) -> AgentStructuredAnswer: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 tests and a Deep Agents compatibility gate</name>
  <files>backend/tests/agent/test_deep_research_subagents.py</files>
  <read_first>
    - backend/tests/agent/test_deep_research_runtime.py
    - backend/tests/agent/test_runtime.py
    - backend/requirements.txt
    - backend/agent/deep_research_runtime.py
  </read_first>
  <behavior>
    - Test 1: a supervisor factory can build a Deep Agents configuration that delegates more than one subquestion through a configured research subagent.
    - Test 2: the research subagent receives retrieval tools named `web_search` and `open_url`; if the repo is still on `web_crawl`, the test allows an `open_url` wrapper around `run_web_crawl`.
    - Test 3: a dependency smoke test fails with a clear assertion message when `deepagents` cannot be imported or the expected constructor surface is unavailable.
  </behavior>
  <action>Create `backend/tests/agent/test_deep_research_subagents.py` with pytest coverage for `fanout`, `retrieval`, and `compatibility` selectors. Include one smoke test that imports `deepagents` with `pytest.importorskip("deepagents")`, asserts `hasattr(deepagents, "create_deep_agent")`, and fails with the exact message `deepagents create_deep_agent API is required for Phase 4` when the constructor is missing. Add one test that expects a research subagent spec named `research_subagent` and asserts `sorted(tool.name for tool in spec["tools"]) == ["open_url", "web_search"]`. Add one test that builds a supervisor via a concrete `build_deep_research_supervisor(...)` function, verifies `create_deep_agent` was called once, and verifies the delegated subagent count equals the number of plan subquestions.</action>
  <acceptance_criteria>
    - `rg -n "deepagents create_deep_agent API is required for Phase 4" backend/tests/agent/test_deep_research_subagents.py` returns 1 match.
    - `rg -n "\\[\\\"open_url\\\", \\\"web_search\\\"\\]" backend/tests/agent/test_deep_research_subagents.py` returns at least 1 match.
    - `rg -n "build_deep_research_supervisor" backend/tests/agent/test_deep_research_subagents.py` returns at least 1 match.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/agent/test_deep_research_subagents.py -q -k "fanout or retrieval or compatibility"</automated>
  </verify>
  <done>The new test file exists, covers RSCH-01 and RSCH-02 fan-out/retrieval behavior, and includes a dependency smoke gate that fails clearly when the Deep Agents stack is unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add typed subagent schemas and shared retrieval-tool builders</name>
  <files>backend/agent/schemas/deep_research_subagent.py, backend/agent/deep_research_retrieval_tools.py</files>
  <read_first>
    - backend/agent/schemas/deep_research.py
    - backend/agent/schemas/__init__.py
    - backend/agent/runtime_sources.py
    - backend/app/tools/web_search.py
    - backend/app/tools/web_crawl.py
  </read_first>
  <behavior>
    - Test 1: a typed subagent result model accepts `subquestion`, `subanswer`, `sources`, `citations`, and optional `artifact_path`.
    - Test 2: a retrieval-tool factory returns exactly two tool objects named `web_search` and `open_url`.
  </behavior>
  <action>Create `backend/agent/schemas/deep_research_subagent.py` with Pydantic models `DeepResearchSubagentAssignment`, `DeepResearchSubagentResult`, and `DeepResearchArtifactRecord`. Use `AgentSourceReference` and `AgentAnswerCitation` from `backend.agent.schemas` for evidence fields, and keep `artifact_path: str | None = None`. Create `backend/agent/deep_research_retrieval_tools.py` with a direct function `build_deep_research_retrieval_tools(*, retrieval_policy: AgentRunRetrievalPolicy, max_search_results: int = 8, max_content_chars: int = 12000) -> tuple[Any, Any]`. Build the first tool via `build_web_search_tool(max_results_cap=max_search_results, retrieval_policy=retrieval_policy)`. Build the second tool as an `@tool("open_url", args_schema=WebCrawlInput)` wrapper that calls `run_web_crawl(url=url, urls=urls, objective=objective, retrieval_policy=retrieval_policy)` so the Deep Agents surface matches the roadmap even while the underlying crawler stays the same. Export these builders directly; do not add generic runner indirection.</action>
  <acceptance_criteria>
    - `rg -n "class DeepResearchSubagentResult" backend/agent/schemas/deep_research_subagent.py` returns 1 match.
    - `rg -n "artifact_path: str \\| None = None" backend/agent/schemas/deep_research_subagent.py` returns 1 match.
    - `rg -n "@tool\\(\"open_url\"" backend/agent/deep_research_retrieval_tools.py` returns 1 match.
    - `rg -n "build_deep_research_retrieval_tools" backend/agent/deep_research_retrieval_tools.py` returns at least 1 match.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/agent/test_deep_research_subagents.py -q -k "retrieval"</automated>
  </verify>
  <done>The repo has typed subagent payload models plus one shared retrieval-tool factory that exposes `web_search` and `open_url` for Deep Agents subagents without changing the underlying retrieval behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create research subagent specs and the Deep Agents supervisor factory</name>
  <files>backend/agent/deep_research_subagents.py, backend/agent/deep_research_supervisor.py</files>
  <read_first>
    - backend/agent/deep_research_runtime.py
    - backend/agent/runtime_constants.py
    - backend/agent/deep_research_retrieval_tools.py
    - backend/agent/schemas/deep_research_subagent.py
    - backend/tests/agent/test_deep_research_subagents.py
  </read_first>
  <behavior>
    - Test 1: a subagent builder returns a spec dict with `name`, `description`, `system_prompt`, and injected tools.
    - Test 2: a supervisor builder calls `create_deep_agent(...)` with the subagent list and a prompt that tells the supervisor to delegate each subquestion independently.
  </behavior>
  <action>Create `backend/agent/deep_research_subagents.py` with `build_research_subagent(*, retrieval_policy: AgentRunRetrievalPolicy, max_search_results: int = 8, max_content_chars: int = 12000) -> dict[str, Any]`. Return a dict with `name="research_subagent"`, `description="Research one assigned subquestion and return a concise subanswer with normalized evidence."`, `system_prompt="You are a deep-research worker. Answer exactly one assigned subquestion. Use web_search first, use open_url for selected pages, and return a concise subanswer plus reusable evidence."`, and `tools=list(build_deep_research_retrieval_tools(...))`. Create `backend/agent/deep_research_supervisor.py` with `build_deep_research_supervisor(*, retrieval_policy: AgentRunRetrievalPolicy, create_agent: Callable[..., Any] | None = None) -> Any` that defaults `create_agent` to `deepagents.create_deep_agent` and passes `subagents=[build_research_subagent(...)]`. Use a supervisor prompt containing the exact sentence `Delegate each independent subquestion to the research_subagent in parallel when the plan contains more than one subquestion.`</action>
  <acceptance_criteria>
    - `rg -n "name=\\\"research_subagent\\\"|\\\"research_subagent\\\"" backend/agent/deep_research_subagents.py` returns at least 1 match.
    - `rg -n "Delegate each independent subquestion to the research_subagent in parallel" backend/agent/deep_research_supervisor.py` returns 1 match.
    - `rg -n "create_deep_agent" backend/agent/deep_research_supervisor.py` returns at least 1 match.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/agent/test_deep_research_subagents.py -q -k "fanout or retrieval"</automated>
  </verify>
  <done>The codebase has a concrete supervisor factory and a reusable research-subagent spec that explicitly injects the existing retrieval behavior into Deep Agents fan-out.</done>
</task>

</tasks>

<verification>
Run `pytest backend/tests/agent/test_deep_research_subagents.py -q` after the plan. The suite must cover the compatibility smoke gate, retrieval-tool names, and supervisor delegation behavior without touching the full backend suite yet.
</verification>

<success_criteria>
Phase 4 has a concrete fan-out foundation: typed subagent models exist, retrieval-tool injection exposes `web_search` plus `open_url`, and a Deep Agents supervisor factory can be built and tested independently from runtime wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deep-agents-research-fan-out/04-01-SUMMARY.md`
</output>
