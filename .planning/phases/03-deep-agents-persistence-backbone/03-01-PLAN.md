---
phase: 03-deep-agents-persistence-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/config.py
  - docker-compose.yml
  - backend/agent/deep_agents/persistence/backend_factory.py
  - backend/agent/deep_agents/persistence/checkpointer.py
  - backend/agent/deep_agents/persistence/store.py
  - backend/agent/deep_agents/persistence/artifacts.py
  - backend/agent/deep_agents/schemas/persisted_plan.py
  - backend/agent/deep_agents/schemas/persisted_status.py
  - backend/tests/conftest.py
  - backend/tests/agent/test_deep_agent_persistence.py
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
user_setup: []
must_haves:
  truths:
    - Deep-research persistence wiring uses official Deep Agents and Postgres packages instead of extending InMemoryDeepResearchStore.
    - The backend has one explicit async persistence spine built from CompositeBackend, AsyncPostgresStore, and AsyncPostgresSaver.
    - Durable plan artifacts are routed into run-scoped storage namespaces with explicit namespace factories.
  artifacts:
    - backend/agent/deep_agents/persistence/backend_factory.py
    - backend/agent/deep_agents/persistence/checkpointer.py
    - backend/agent/deep_agents/persistence/store.py
    - backend/agent/deep_agents/persistence/artifacts.py
    - backend/agent/deep_agents/schemas/persisted_plan.py
    - backend/agent/deep_agents/schemas/persisted_status.py
    - backend/tests/agent/test_deep_agent_persistence.py
  key_links:
    - backend/requirements.txt adds Deep Agents and Postgres persistence dependencies consumed by the new persistence modules.
    - backend/app/config.py exposes the Postgres connection settings used by the store and checkpointer factories.
    - backend/agent/deep_agents/persistence/backend_factory.py routes /runs/ and /plans/ artifact paths into StoreBackend namespaces while scratch paths remain in StateBackend.
---

<objective>
Build the persistence foundation for Phase 3 before runtime integration begins.

Purpose: Replace the placeholder in-memory persistence assumption with concrete dependency, configuration, schema, and backend-factory seams that match the Deep Agents and LangGraph Postgres docs.
Output: Persistence modules, persisted artifact schemas, Postgres settings, local compose wiring, and Wave 0 coverage for durable plan artifacts.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deep-agents-persistence-backbone/03-RESEARCH.md
@.planning/phases/03-deep-agents-persistence-backbone/03-VALIDATION.md
@AGENTS.md
@backend/requirements.txt
@backend/app/config.py
@docker-compose.yml
@backend/agent/deep_research_store.py
@backend/agent/schemas/deep_research.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align backend dependencies and Postgres runtime configuration</name>
  <files>backend/requirements.txt, backend/app/config.py, docker-compose.yml</files>
  <read_first>AGENTS.md, .planning/phases/03-deep-agents-persistence-backbone/03-RESEARCH.md, backend/requirements.txt, backend/app/config.py, docker-compose.yml, backend/Dockerfile</read_first>
  <action>Update backend dependencies to add the official persistence stack with exact packages `deepagents==0.4.12`, `langgraph-checkpoint-postgres==3.0.5`, `langgraph==1.1.3`, `langchain==1.2.13`, `langchain-openai==1.1.12`, and `psycopg[binary]==3.3.3`. Extend `backend/app/config.py` so `Settings` exposes a required `DEEP_RESEARCH_DATABASE_URL` plus optional namespacing values if needed for run prefixes, while preserving existing `OPENAI_API_KEY` and `SERPER_API_KEY` fields. Update `docker-compose.yml` to provide `DEEP_RESEARCH_DATABASE_URL=postgresql://postgres:postgres@postgres:5432/web_agent`, add a `postgres` service with a healthcheck, and make `backend` depend on that service becoming healthy.</action>
  <verify>rg -n "deepagents==0.4.12|langgraph-checkpoint-postgres==3.0.5|langchain==1.2.13|langgraph==1.1.3|langchain-openai==1.1.12|psycopg\\[binary\\]==3.3.3|DEEP_RESEARCH_DATABASE_URL|postgresql://postgres:postgres@postgres:5432/web_agent|postgres:" backend/requirements.txt backend/app/config.py docker-compose.yml</verify>
  <acceptance_criteria>
    - `backend/requirements.txt` contains `deepagents==0.4.12`
    - `backend/requirements.txt` contains `langgraph-checkpoint-postgres==3.0.5`
    - `backend/requirements.txt` contains `psycopg[binary]==3.3.3`
    - `backend/app/config.py` contains `DEEP_RESEARCH_DATABASE_URL`
    - `docker-compose.yml` contains a top-level `postgres:` service
    - `docker-compose.yml` contains `postgresql://postgres:postgres@postgres:5432/web_agent`
  </acceptance_criteria>
  <done>Dependency and runtime configuration files declare the exact Deep Agents and Postgres persistence stack needed by later runtime work.</done>
</task>

<task type="auto">
  <name>Task 2: Add atomic persistence modules and typed artifact schemas</name>
  <files>backend/agent/deep_agents/persistence/backend_factory.py, backend/agent/deep_agents/persistence/checkpointer.py, backend/agent/deep_agents/persistence/store.py, backend/agent/deep_agents/persistence/artifacts.py, backend/agent/deep_agents/schemas/persisted_plan.py, backend/agent/deep_agents/schemas/persisted_status.py</files>
  <read_first>AGENTS.md, .planning/phases/03-deep-agents-persistence-backbone/03-RESEARCH.md, backend/agent/deep_research_store.py, backend/agent/schemas/deep_research.py, backend/app/config.py</read_first>
  <action>Create a focused `backend/agent/deep_agents/` subtree instead of enlarging `backend/agent/deep_research_runtime.py`. Implement `backend_factory.py` with a direct helper that returns `CompositeBackend(default=StateBackend(runtime), routes={"/runs/": StoreBackend(...), "/plans/": StoreBackend(...)})` and uses explicit namespace lambdas that scope artifacts to tuples like `("deep_research", run_id)`. Implement `checkpointer.py` with async helpers that build and initialize `AsyncPostgresSaver.from_conn_string(settings.DEEP_RESEARCH_DATABASE_URL)` and call `await saver.setup()`. Implement `store.py` with parallel async helpers for `AsyncPostgresStore.from_conn_string(...)` and `await store.setup()`. Implement `artifacts.py` with concrete path builders for `/runs/{run_id}/plan.md`, `/runs/{run_id}/subquestions.json`, `/runs/{run_id}/status.json`, and `/runs/{run_id}/final_answer.md`. Define Pydantic artifact models in `backend/agent/deep_agents/schemas/persisted_plan.py` and `backend/agent/deep_agents/schemas/persisted_status.py` so persisted plan and status shapes are typed instead of raw dicts.</action>
  <verify>rg -n "CompositeBackend|StateBackend|StoreBackend|AsyncPostgresSaver|AsyncPostgresStore|plan\\.md|subquestions\\.json|status\\.json|final_answer\\.md|namespace=lambda" backend/agent/deep_agents</verify>
  <acceptance_criteria>
    - `backend/agent/deep_agents/persistence/backend_factory.py` contains `CompositeBackend(`
    - `backend/agent/deep_agents/persistence/backend_factory.py` contains `namespace=lambda`
    - `backend/agent/deep_agents/persistence/checkpointer.py` contains `AsyncPostgresSaver`
    - `backend/agent/deep_agents/persistence/store.py` contains `AsyncPostgresStore`
    - `backend/agent/deep_agents/persistence/artifacts.py` contains `/runs/{run_id}/plan.md`
    - `backend/agent/deep_agents/schemas/persisted_plan.py` defines a Pydantic model for the persisted plan artifact
    - `backend/agent/deep_agents/schemas/persisted_status.py` defines a Pydantic model for the persisted status artifact
  </acceptance_criteria>
  <done>The repo has small, task-focused persistence modules and typed artifact schemas that later runtime integration can call directly.</done>
</task>

<task type="auto">
  <name>Task 3: Add Wave 0 persistence coverage and Postgres-aware fixtures</name>
  <files>backend/tests/conftest.py, backend/tests/agent/test_deep_agent_persistence.py</files>
  <read_first>.planning/phases/03-deep-agents-persistence-backbone/03-VALIDATION.md, backend/tests/conftest.py, backend/tests/agent/test_deep_research_runtime.py, backend/agent/deep_agents/persistence/backend_factory.py, backend/agent/deep_agents/persistence/artifacts.py</read_first>
  <action>Add backend test helpers that can construct the new persistence modules without touching production globals. Create `backend/tests/agent/test_deep_agent_persistence.py` with concrete assertions that artifact path helpers emit `/runs/{run_id}/plan.md`, `/runs/{run_id}/subquestions.json`, `/runs/{run_id}/status.json`, and `/runs/{run_id}/final_answer.md`; that backend routing sends those paths through `StoreBackend`; and that namespace builders scope artifacts by `run_id` rather than shared global keys. Keep this plan’s tests focused on artifact shape and routing so runtime resume stays in Plan 02.</action>
  <verify>pytest backend/tests/agent/test_deep_agent_persistence.py -q</verify>
  <acceptance_criteria>
    - `backend/tests/agent/test_deep_agent_persistence.py` exists
    - `backend/tests/agent/test_deep_agent_persistence.py` contains `plan.md`
    - `backend/tests/agent/test_deep_agent_persistence.py` contains `status.json`
    - `pytest backend/tests/agent/test_deep_agent_persistence.py -q` exits `0`
  </acceptance_criteria>
  <done>Wave 0 test infrastructure exists for persistence artifacts and routing before runtime integration changes land.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest backend/tests/agent/test_deep_agent_persistence.py -q`
- [ ] `pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/api/test_agent_run_service.py -q`
- [ ] `docker compose config` renders a healthy `postgres` service and backend env wiring
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has an explicit persistence foundation with official Deep Agents and Postgres packages
- Durable artifact paths and namespace routing are implemented in focused modules, not hidden behind the legacy in-memory store

  </success_criteria>

<output>
After completion, create `.planning/phases/03-deep-agents-persistence-backbone/03-01-SUMMARY.md`
</output>
