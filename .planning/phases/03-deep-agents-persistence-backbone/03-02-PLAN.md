---
phase: 03-deep-agents-persistence-backbone
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - backend/agent/deep_research_runtime.py
  - backend/api/services/agent_run.py
  - backend/agent/deep_agents/supervisor.py
  - backend/agent/deep_agents/resume.py
  - backend/agent/deep_agents/logging.py
  - backend/agent/deep_agents/persistence/artifacts.py
  - backend/tests/agent/test_deep_research_runtime.py
  - backend/tests/agent/test_deep_agent_resume.py
  - backend/tests/agent/test_deep_agent_logging.py
  - backend/tests/api/test_agent_run_service.py
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
user_setup: []
must_haves:
  truths:
    - Starting a deep-research run persists a high-level plan artifact before research fan-out begins.
    - The same persisted plan can be read to derive subquestions or research angles.
    - A deep-research run can resume with the same thread identifier using LangGraph Postgres checkpoints.
    - Persisted status artifacts and structured logs expose progress without relying on the frontend in-memory run history store.
  artifacts:
    - backend/agent/deep_agents/supervisor.py
    - backend/agent/deep_agents/resume.py
    - backend/agent/deep_agents/logging.py
    - backend/tests/agent/test_deep_agent_resume.py
    - backend/tests/agent/test_deep_agent_logging.py
  key_links:
    - backend/api/services/agent_run.py passes stable run and thread metadata into the deep-research supervisor entrypoint.
    - backend/agent/deep_research_runtime.py delegates persistence and resume work to the new deep_agents modules instead of owning monolithic orchestration.
    - backend/agent/deep_agents/logging.py mirrors each stage transition into persisted status artifacts and structured log records keyed by run_id and thread_id.
---

<objective>
Integrate the persistence backbone into the deep-research runtime and make progress inspectable and resumable.

Purpose: Turn the new persistence modules into real runtime behavior that satisfies Phase 3 user outcomes for persistent planning, derived subquestions, resume, and inspectable progress.
Output: Deep-research supervisor/resume modules, runtime entrypoint integration, persisted plan and status writes, and resume/progress test coverage.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deep-agents-persistence-backbone/03-RESEARCH.md
@.planning/phases/03-deep-agents-persistence-backbone/03-VALIDATION.md
@AGENTS.md
@backend/agent/deep_research_runtime.py
@backend/api/services/agent_run.py
@backend/agent/schemas/deep_research.py
@backend/tests/agent/test_deep_research_runtime.py
@backend/tests/api/test_agent_run_service.py
@backend/agent/deep_agents/persistence/backend_factory.py
@backend/agent/deep_agents/persistence/checkpointer.py
@backend/agent/deep_agents/persistence/store.py
@backend/agent/deep_agents/persistence/artifacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder deep-research planning state with persisted supervisor artifacts</name>
  <files>backend/agent/deep_research_runtime.py, backend/agent/deep_agents/supervisor.py, backend/agent/deep_agents/persistence/artifacts.py, backend/tests/agent/test_deep_research_runtime.py</files>
  <read_first>AGENTS.md, .planning/phases/03-deep-agents-persistence-backbone/03-RESEARCH.md, backend/agent/deep_research_runtime.py, backend/agent/deep_research_planning.py, backend/agent/schemas/deep_research.py, backend/agent/deep_agents/persistence/artifacts.py, backend/tests/agent/test_deep_research_runtime.py</read_first>
  <action>Create `backend/agent/deep_agents/supervisor.py` with direct async entrypoints that assemble `create_deep_agent(...)` using the Postgres store/checkpointer from Plan 01 and persist the canonical plan to `/runs/{run_id}/plan.md` before any research wave work begins. Persist derived subquestions to `/runs/{run_id}/subquestions.json`, then update `backend/agent/deep_research_runtime.py` so the planning stage calls the new supervisor entrypoint rather than `build_deep_research_plan(job)` returning only an in-memory list. Keep `backend/agent/deep_research_runtime.py` as a thin coordinator that invokes concrete helpers directly.</action>
  <verify>pytest backend/tests/agent/test_deep_research_runtime.py -q</verify>
  <acceptance_criteria>
    - `backend/agent/deep_agents/supervisor.py` contains `create_deep_agent(`
    - `backend/agent/deep_research_runtime.py` no longer contains the direct default `plan_builder: Callable[[DeepResearchJob], list[str] | DeepResearchPlan] = build_deep_research_plan`
    - `backend/agent/deep_research_runtime.py` contains a direct call into `backend.agent.deep_agents.supervisor`
    - `backend/tests/agent/test_deep_research_runtime.py` asserts plan persistence before wave execution
    - `pytest backend/tests/agent/test_deep_research_runtime.py -q` exits `0`
  </acceptance_criteria>
  <done>Deep-research planning is persisted as durable artifacts and runtime orchestration stays explicit and modular.</done>
</task>

<task type="auto">
  <name>Task 2: Add checkpoint resume and stable thread metadata through the API entry path</name>
  <files>backend/api/services/agent_run.py, backend/agent/deep_research_runtime.py, backend/agent/deep_agents/resume.py, backend/tests/agent/test_deep_agent_resume.py, backend/tests/api/test_agent_run_service.py</files>
  <read_first>AGENTS.md, .planning/phases/03-deep-agents-persistence-backbone/03-RESEARCH.md, backend/api/services/agent_run.py, backend/agent/deep_research_runtime.py, backend/tests/api/test_agent_run_service.py, backend/tests/agent/test_deep_research_runtime.py</read_first>
  <action>Add a concrete resume seam that carries both `run_id` and stable `thread_id` through deep-research startup. Implement `backend/agent/deep_agents/resume.py` with direct helpers that open the Postgres saver/store, reuse the same thread identifier, and resume from checkpoints instead of creating a fresh in-memory job. Update `backend/api/services/agent_run.py` so deep-research requests call a runtime entrypoint that can create or resume the same thread while preserving the existing queued HTTP envelope. Add tests that prove a resumed call with the same thread metadata restores checkpoint-backed state instead of resetting subquestions or stage.</action>
  <verify>pytest backend/tests/agent/test_deep_agent_resume.py backend/tests/api/test_agent_run_service.py -q</verify>
  <acceptance_criteria>
    - `backend/agent/deep_agents/resume.py` exists
    - `backend/agent/deep_agents/resume.py` contains `thread_id`
    - `backend/api/services/agent_run.py` contains a deep-research path that passes stable resume metadata
    - `backend/tests/agent/test_deep_agent_resume.py` exists
    - `pytest backend/tests/agent/test_deep_agent_resume.py backend/tests/api/test_agent_run_service.py -q` exits `0`
  </acceptance_criteria>
  <done>The API and runtime can resume deep-research work from persisted checkpoints instead of starting over.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror stage transitions into persisted status artifacts and structured logs</name>
  <files>backend/agent/deep_agents/logging.py, backend/agent/deep_research_runtime.py, backend/agent/deep_agents/persistence/artifacts.py, backend/tests/agent/test_deep_agent_logging.py</files>
  <read_first>.planning/phases/03-deep-agents-persistence-backbone/03-RESEARCH.md, .planning/phases/03-deep-agents-persistence-backbone/03-VALIDATION.md, backend/agent/deep_research_runtime.py, backend/agent/runtime_execution.py, backend/agent/deep_agents/persistence/artifacts.py</read_first>
  <action>Implement `backend/agent/deep_agents/logging.py` with direct helpers that write typed status snapshots to `/runs/{run_id}/status.json` and emit structured log records containing exact keys `run_id`, `thread_id`, `stage`, and `artifact_path`. Update the deep-research runtime coordinator so planning start, plan persisted, searching, verifying, synthesizing, completion, and failure each call the logging helper and update the persisted status artifact. Add `backend/tests/agent/test_deep_agent_logging.py` to assert the status artifact content and log payload keys. Do not route PLAN-04 through the frontend run-history store.</action>
  <verify>pytest backend/tests/agent/test_deep_agent_logging.py -q</verify>
  <acceptance_criteria>
    - `backend/agent/deep_agents/logging.py` contains `run_id`
    - `backend/agent/deep_agents/logging.py` contains `thread_id`
    - `backend/agent/deep_agents/logging.py` contains `status.json`
    - `backend/agent/deep_research_runtime.py` contains calls that update persisted status for `planning`, `searching`, `verifying`, `synthesizing`, `completed`, and `failed`
    - `backend/tests/agent/test_deep_agent_logging.py` exists
    - `pytest backend/tests/agent/test_deep_agent_logging.py -q` exits `0`
  </acceptance_criteria>
  <done>Progress is inspectable from persisted artifacts and structured logs even if the frontend process restarts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest backend/tests/agent/test_deep_research_runtime.py backend/tests/agent/test_deep_agent_resume.py backend/tests/agent/test_deep_agent_logging.py -q`
- [ ] `pytest backend/tests/api/test_agent_run_service.py backend/tests/api/test_agent_run_route.py -q`
- [ ] `pytest backend/tests -q`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Deep-research runs persist plan and subquestion artifacts before fan-out
- Resume uses checkpoint-backed thread state rather than a process-local in-memory store
- Progress inspection works through persisted status artifacts and structured logs without new frontend work

  </success_criteria>

<output>
After completion, create `.planning/phases/03-deep-agents-persistence-backbone/03-02-SUMMARY.md`
</output>
