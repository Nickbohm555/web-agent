---
phase: 05-evidence-grounded-completion
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - backend/agent/deep_research_runtime.py
  - backend/agent/deep_research_verification.py
  - backend/agent/deep_research_completion.py
  - backend/agent/deep_research_finalization.py
  - backend/agent/schemas/deep_research.py
  - backend/tests/agent/test_deep_research_runtime.py
  - backend/tests/agent/test_runtime.py
autonomous: true
requirements:
  - RSCH-04
  - DEEP-04
  - TOOL-03
user_setup: []
must_haves:
  truths:
    - The supervisor keeps researching until each planned subquestion has a sufficient normalized result or a bounded retry budget is exhausted.
    - Final deep-research synthesis occurs only after the sufficiency gate passes.
    - The top-level `deep_research` API envelope remains unchanged while evidence is carried inside `final_answer`.
  artifacts:
    - backend/agent/deep_research_completion.py exists with sufficiency evaluation logic.
    - backend/agent/deep_research_finalization.py exists with final-answer assembly logic.
    - backend/tests/agent/test_deep_research_runtime.py covers repeated research passes and completion gating.
  key_links:
    - `run_deep_research_job(...)` calls the sufficiency evaluator before finalization.
    - Finalization emits the same `AgentStructuredAnswer` and `AgentRunSuccessResponse` shape already exposed by the API.
---

<objective>
Implement supervisor-side extraction, sufficiency gating, and final completion so deep research answers ship only when the planned subquestions are answered well enough.

Purpose: Phase 5’s user-facing value is not the middleware alone; it is the completion gate that keeps the final answer evidence-grounded without changing the API contract.
Output: Small completion/finalization modules, a repeated research loop, and tests that prove synthesis waits for sufficient evidence while preserving the existing response shape.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-evidence-grounded-completion/05-RESEARCH.md
@.planning/phases/05-evidence-grounded-completion/05-VALIDATION.md
@AGENTS.md
@backend/agent/deep_research_runtime.py
@backend/agent/deep_research_verification.py
@backend/agent/schemas/deep_research.py
@backend/api/schemas/agent_run.py
@backend/agent/schemas/__init__.py
@backend/tests/agent/test_deep_research_runtime.py
@backend/tests/agent/test_runtime.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sufficiency evaluation and repeated research gating</name>
  <files>backend/agent/deep_research_completion.py, backend/agent/deep_research_verification.py, backend/agent/deep_research_runtime.py, backend/agent/schemas/deep_research.py, backend/tests/agent/test_deep_research_runtime.py</files>
  <read_first>backend/agent/deep_research_runtime.py, backend/agent/deep_research_verification.py, backend/agent/schemas/deep_research.py, backend/tests/agent/test_deep_research_runtime.py, .planning/phases/05-evidence-grounded-completion/05-01-PLAN.md, AGENTS.md</read_first>
  <action>Create `backend/agent/deep_research_completion.py` with direct functions named `evaluate_deep_research_sufficiency(...)` and `next_deep_research_subquestions(...)`. Extend `DeepResearchJob` so it can store normalized subagent results plus a bounded retry counter such as `research_pass_count` and `max_research_passes` with the default value `3`. Update `backend/agent/deep_research_verification.py` so verification returns a concrete sufficiency decision instead of only changing the stage. Update `backend/agent/deep_research_runtime.py` so `run_deep_research_job(...)` loops: plan initial subquestions, execute research, normalize results, evaluate sufficiency, and if any planned subquestion still lacks a non-empty `subanswer` or any evidence-bearing references, run another research pass only for unresolved subquestions until sufficiency passes or `max_research_passes` is reached. On exhaustion, fail the job with `category=\"tool_failure\"` and a message that evidence remained insufficient.</action>
  <verify>pytest backend/tests/agent/test_deep_research_runtime.py -k "sufficiency or retry or incomplete"</verify>
  <acceptance_criteria>
    - `backend/agent/deep_research_completion.py` contains `def evaluate_deep_research_sufficiency(`
    - `backend/agent/schemas/deep_research.py` contains `research_pass_count`
    - `backend/agent/schemas/deep_research.py` contains `max_research_passes`
    - `backend/agent/deep_research_runtime.py` contains `max_research_passes`
    - `backend/tests/agent/test_deep_research_runtime.py` contains `insufficient evidence`
  </acceptance_criteria>
  <done>The deep-research supervisor has a bounded sufficiency loop and cannot synthesize while required subquestions still lack evidence-bearing results.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize evidence-grounded answers without changing the top-level API envelope</name>
  <files>backend/agent/deep_research_finalization.py, backend/agent/deep_research_runtime.py, backend/agent/schemas/deep_research.py, backend/tests/agent/test_deep_research_runtime.py, backend/tests/agent/test_runtime.py</files>
  <read_first>backend/agent/deep_research_runtime.py, backend/agent/schemas/__init__.py, backend/api/schemas/agent_run.py, backend/tests/agent/test_deep_research_runtime.py, backend/tests/agent/test_runtime.py, docs/superpowers/specs/2026-03-24-agent-crawl-backed-citations-design.md</read_first>
  <action>Create `backend/agent/deep_research_finalization.py` with direct functions such as `build_deep_research_final_answer(...)` and `build_deep_research_sources(...)`. Finalize from normalized subagent results only after sufficiency passes. Keep `AgentRunSuccessResponse` unchanged: `run_id`, `status`, `final_answer`, `sources`, `tool_call_count`, `elapsed_ms`, and `metadata` stay as they are today. Put evidence into `final_answer.basis` and top-level `final_answer.citations`, using crawl citations as answer-eligible evidence and search sources as supervisor metadata only. Update runtime tests so a deep-research completion still validates against the existing response shape while the resulting `final_answer` now carries basis/citation evidence assembled from normalized subagent results.</action>
  <verify>pytest backend/tests/agent/test_deep_research_runtime.py -k "final_answer or basis or citations" && pytest backend/tests/agent/test_runtime.py -k deep_research</verify>
  <acceptance_criteria>
    - `backend/agent/deep_research_finalization.py` contains `def build_deep_research_final_answer(`
    - `backend/api/schemas/agent_run.py` still contains `class AgentRunSuccessResponse`
    - `backend/tests/agent/test_runtime.py` contains `deep_research`
    - `backend/tests/agent/test_deep_research_runtime.py` contains `final_answer`
    - `backend/tests/agent/test_deep_research_runtime.py` contains `basis`
  </acceptance_criteria>
  <done>Deep research answers are finalized from normalized evidence only after the sufficiency gate passes, and the public API response envelope remains unchanged.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest backend/tests/agent/test_deep_research_runtime.py -k "sufficiency or retry or final_answer or basis or citations"` passes
- [ ] `pytest backend/tests/agent/test_runtime.py -k deep_research` passes
- [ ] `rg -n "evaluate_deep_research_sufficiency|build_deep_research_final_answer|max_research_passes|tool_failure" backend/agent backend/tests/agent backend/tests/agent/test_runtime.py` returns the new gating and finalization seams
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Deep research completion is explicitly gated on sufficient normalized evidence rather than a one-pass placeholder finalizer
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence-grounded-completion/05-02-SUMMARY.md`
</output>
