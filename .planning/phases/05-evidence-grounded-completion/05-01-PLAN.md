---
phase: 05-evidence-grounded-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/schemas/deep_research.py
  - backend/agent/deep_research_execution.py
  - backend/agent/prompts.py
  - backend/agent/deep_research_subagent_middleware.py
  - backend/agent/deep_research_result_normalization.py
  - backend/tests/agent/test_deep_research_runtime.py
autonomous: true
requirements:
  - RSCH-03
user_setup: []
must_haves:
  truths:
    - Deep research subagent results are validated against an explicit repo-local schema before the supervisor accepts them.
    - Each accepted subagent result carries a subanswer plus normalized search-source metadata and crawl-citation evidence.
  artifacts:
    - backend/agent/deep_research_subagent_middleware.py exists with middleware-specific enforcement logic.
    - backend/agent/deep_research_result_normalization.py exists with typed normalization helpers.
    - backend/tests/agent/test_deep_research_runtime.py covers malformed and valid subagent outputs.
  key_links:
    - Deep research execution uses the middleware helper before results are normalized.
    - The normalized result schema reuses existing citation and source contracts instead of raw dictionaries.
---

<objective>
Define the evidence-bearing Deep Agents subagent contract and enforce it before supervisor synthesis logic consumes any result.

Purpose: Phase 5 cannot gate completion on evidence until subagent outputs are explicit, normalized, and validated.
Output: Typed deep-research result schemas, a focused middleware module, execution wiring, and failing/passing tests for malformed versus valid subagent outputs.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-evidence-grounded-completion/05-RESEARCH.md
@.planning/phases/05-evidence-grounded-completion/05-VALIDATION.md
@AGENTS.md
@backend/agent/schemas/deep_research.py
@backend/agent/deep_research_execution.py
@backend/agent/prompts.py
@backend/agent/runtime_sources.py
@backend/tests/agent/test_deep_research_runtime.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed deep-research subagent evidence models and normalization helpers</name>
  <files>backend/agent/schemas/deep_research.py, backend/agent/deep_research_result_normalization.py, backend/tests/agent/test_deep_research_runtime.py</files>
  <read_first>backend/agent/schemas/deep_research.py, backend/agent/schemas/__init__.py, backend/agent/runtime_sources.py, backend/tests/agent/test_deep_research_runtime.py, AGENTS.md</read_first>
  <action>Create explicit Pydantic models in `backend/agent/schemas/deep_research.py` for the Deep Research subagent result contract. Add concrete types named `DeepResearchSearchSource`, `DeepResearchCrawlCitation`, `DeepResearchSubagentAnswer`, and `DeepResearchSubagentResult`. `DeepResearchSubagentAnswer` must include `subanswer: str`, `search_sources: list[DeepResearchSearchSource]`, and `crawl_citations: list[DeepResearchCrawlCitation]`. `DeepResearchSubagentResult` must include the planned subquestion identifier or text plus the normalized answer payload and a `sufficient: bool` field. Add `backend/agent/deep_research_result_normalization.py` with direct functions such as `normalize_deep_research_subagent_result(...)` and `normalize_deep_research_subagent_results(...)` that convert raw subagent returns into these models while reusing existing source/citation validation patterns from `runtime_sources.py`. Extend `backend/tests/agent/test_deep_research_runtime.py` with tests that reject missing `subanswer`, missing `search_sources`, and missing `crawl_citations`, and accept a fully populated result.</action>
  <verify>pytest backend/tests/agent/test_deep_research_runtime.py -k "subagent or normalization"</verify>
  <acceptance_criteria>
    - `backend/agent/schemas/deep_research.py` contains `class DeepResearchSubagentResult`
    - `backend/agent/schemas/deep_research.py` contains `class DeepResearchSubagentAnswer`
    - `backend/agent/deep_research_result_normalization.py` contains `def normalize_deep_research_subagent_result(`
    - `backend/tests/agent/test_deep_research_runtime.py` contains `missing crawl_citations`
  </acceptance_criteria>
  <done>Typed subagent-result models exist, normalization helpers exist, and targeted tests prove malformed payloads fail validation.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused middleware enforcement and wire execution to use it</name>
  <files>backend/agent/deep_research_subagent_middleware.py, backend/agent/deep_research_execution.py, backend/agent/prompts.py, backend/tests/agent/test_deep_research_runtime.py</files>
  <read_first>backend/agent/deep_research_execution.py, backend/agent/prompts.py, backend/agent/runtime_policy.py, backend/tests/agent/test_deep_research_runtime.py, AGENTS.md</read_first>
  <action>Create `backend/agent/deep_research_subagent_middleware.py` as a small module dedicated to Phase 5 enforcement. Add a direct helper such as `build_deep_research_subagent_middleware()` or `enforce_deep_research_subagent_contract(...)` that injects the exact required contract into subagent instructions and rejects or marks incomplete any subagent return that does not supply a non-empty `subanswer`, at least one normalized `search_sources` entry, and at least one normalized `crawl_citations` entry. Update `backend/agent/prompts.py` so deep-research guidance explicitly requires the subagent result keys `subanswer`, `search_sources`, and `crawl_citations`. Update `backend/agent/deep_research_execution.py` so the eventual Deep Agents task/subagent path calls the middleware helper before passing results into normalization. Keep the call path explicit; do not hide the enforcement behind a generic runner abstraction.</action>
  <verify>pytest backend/tests/agent/test_deep_research_runtime.py -k "middleware or contract"</verify>
  <acceptance_criteria>
    - `backend/agent/deep_research_subagent_middleware.py` contains `def enforce_deep_research_subagent_contract(`
    - `backend/agent/prompts.py` contains `search_sources`
    - `backend/agent/prompts.py` contains `crawl_citations`
    - `backend/agent/deep_research_execution.py` contains `enforce_deep_research_subagent_contract`
  </acceptance_criteria>
  <done>Execution wiring uses a dedicated middleware-enforcement module and deep-research prompt guidance names the exact result keys the subagent must return.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest backend/tests/agent/test_deep_research_runtime.py -k "subagent or normalization or middleware"` passes
- [ ] `rg -n "DeepResearchSubagentResult|enforce_deep_research_subagent_contract|crawl_citations|search_sources" backend/agent backend/tests/agent/test_deep_research_runtime.py` returns the new contract wiring
- [ ] The new logic lives in focused modules rather than expanding `backend/agent/deep_research_runtime.py` into a monolith
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Deep research has an explicit evidence-bearing subagent result contract before supervisor completion logic is implemented
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence-grounded-completion/05-01-SUMMARY.md`
</output>
