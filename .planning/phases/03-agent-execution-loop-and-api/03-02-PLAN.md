---
phase: 03-agent-execution-loop-and-api
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/api/contracts.py
  - backend/api/errors.py
  - backend/api/routes/agent_run.py
  - backend/main.py
  - backend/tests/api/test_agent_run_route.py
autonomous: true
must_haves:
  truths:
    - "Frontend-callable endpoint accepts a prompt and triggers one agent run."
    - "Single request returns the final answer from that same run."
    - "API envelope remains stable for frontend rendering of answer plus run metadata."
    - "Expected failures are surfaced with explicit, debuggable error responses."
  artifacts:
    - path: "backend/api/contracts.py"
      provides: "Typed request/response models with a stable run envelope."
    - path: "backend/api/routes/agent_run.py"
      provides: "POST execution route that invokes runtime once per request."
    - path: "backend/api/errors.py"
      provides: "Runtime-to-HTTP error mapping and error payload model."
    - path: "backend/main.py"
      provides: "FastAPI app wiring that exposes the run route."
    - path: "backend/tests/api/test_agent_run_route.py"
      provides: "Route contract and integration tests for success and failure paths."
  key_links:
    - from: "backend/api/routes/agent_run.py"
      to: "backend/agent/runtime.py"
      via: "single call to runtime facade per request"
      pattern: "run_agent_once"
    - from: "backend/api/routes/agent_run.py"
      to: "backend/api/contracts.py"
      via: "response_model enforcement of stable envelope"
      pattern: "response_model"
    - from: "backend/api/errors.py"
      to: "backend/agent/types.py"
      via: "typed runtime error to HTTP/status mapping"
      pattern: "error_code|status_code"
---

<objective>
Implement the API seam for Phase 03 by exposing a frontend-callable run endpoint with stable contracts and explicit error mapping over the runtime created in 03-01.

Purpose: Satisfy RUNTIME-03 and AGENT-04 transport requirements while preserving separation between runtime internals and external response contract.
Output: One production-ready `POST /api/agent/run` path with validated request model, stable response envelope, and deterministic failure mapping.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-execution-loop-and-api/03-RESEARCH.md
@.planning/phases/03-agent-execution-loop-and-api/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define stable API request/response contracts and error envelope</name>
  <files>backend/api/contracts.py, backend/api/errors.py</files>
  <action>Create contract models for request/response with additive-safe top-level fields and strict validation on required keys. Include stable success envelope fields (`run_id`, `status`, `final_answer`, `tool_call_count`, `elapsed_ms`) plus a structured metadata object for future observability expansion. Define explicit API error payload shape and map runtime error categories from 03-01 to deterministic HTTP status and machine-readable error codes.</action>
  <verify>Run contract-focused tests: `pytest backend/tests/api/test_agent_run_route.py -q -k "contract or validation or error"`.</verify>
  <done>Invalid request payloads fail with schema errors, success responses always include required stable fields, and known runtime failures map to explicit API error contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement run endpoint wiring and app integration</name>
  <files>backend/api/routes/agent_run.py, backend/main.py</files>
  <action>Add `POST /api/agent/run` route that validates request body, calls runtime exactly once, and returns only contract-approved fields. Wire route into FastAPI app startup path and ensure endpoint is reachable by frontend without exposing runtime internals. Do not add streaming, queues, or multi-run orchestration in this phase; keep one synchronous request-response flow.</action>
  <verify>Start backend and execute contract smoke test: `uvicorn backend.main:app --reload` then `curl -s -X POST http://localhost:8000/api/agent/run -H 'content-type: application/json' -d '{"prompt":"find one source and summarize"}'`.</verify>
  <done>Endpoint is reachable, executes one agent run per request, and returns stable answer + metadata envelope suitable for frontend rendering.</done>
</task>

<task type="auto">
  <name>Task 3: Add route integration tests for one-run success and bounded failure paths</name>
  <files>backend/tests/api/test_agent_run_route.py</files>
  <action>Add tests that prove: (1) request validation behavior, (2) successful run envelope stability with required keys/types, (3) runtime recursion/failure mapping into explicit API errors. Stub or fixture runtime as needed to keep tests deterministic and independent from live provider calls.</action>
  <verify>Execute full API route test file: `pytest backend/tests/api/test_agent_run_route.py -q`.</verify>
  <done>Route behavior is deterministic under success and failure modes, and contract regressions fail tests immediately.</done>
</task>

</tasks>

<verification>
Validate end-to-end for this phase seam:
- API accepts prompt from HTTP client.
- API triggers one bounded runtime invocation.
- API returns stable final answer envelope or explicit mapped error.
</verification>

<success_criteria>
- `POST /api/agent/run` is frontend-callable and contract-governed.
- Final answer and run metadata are returned in one response for each request.
- Error surface is explicit and debuggable, with no opaque generic failure contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-execution-loop-and-api/03-02-SUMMARY.md`.
</output>
