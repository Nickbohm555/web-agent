---
phase: 03-agent-execution-loop-and-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/runtime.py
  - backend/agent/prompts.py
  - backend/agent/types.py
  - backend/tests/agent/test_runtime.py
autonomous: true
must_haves:
  truths:
    - "One prompt can run through a single ReAct-style agent execution."
    - "The agent can invoke `web_search` and `web_crawl` in sequence before answering."
    - "Agent runs terminate via explicit loop bounds rather than unbounded iteration."
    - "Runtime emits normalized run result data that API layer can map without exposing provider internals."
  artifacts:
    - path: "backend/agent/runtime.py"
      provides: "LangGraph/LangChain runtime facade that executes one bounded run."
    - path: "backend/agent/prompts.py"
      provides: "System instructions that enforce tool usage behavior and stop conditions."
    - path: "backend/agent/types.py"
      provides: "Stable internal run-result types used by API mapping."
    - path: "backend/tests/agent/test_runtime.py"
      provides: "Runtime tests for bounded execution, tool sequence, and normalized output."
  key_links:
    - from: "backend/agent/runtime.py"
      to: "backend/tools/web_search.py"
      via: "bound tool registry with canonical name `web_search`"
      pattern: "web_search"
    - from: "backend/agent/runtime.py"
      to: "backend/tools/web_crawl.py"
      via: "bound tool registry with canonical name `web_crawl`"
      pattern: "web_crawl"
    - from: "backend/agent/runtime.py"
      to: "LangGraph invoke config"
      via: "recursion_limit/iteration guard passed on each run"
      pattern: "recursion_limit"
---

<objective>
Implement the runtime seam for Phase 03 by wiring a single ReAct-style LangGraph execution loop to the two Phase 2 tools and returning a normalized internal run result.

Purpose: Satisfy AGENT-01 and AGENT-04 core behavior before transport concerns by isolating orchestration in a dedicated runtime module.
Output: A bounded, test-covered `run_agent_once` runtime API ready for backend route integration.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-execution-loop-and-api/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LangGraph runtime facade with canonical tool binding</name>
  <files>backend/agent/runtime.py, backend/agent/prompts.py, backend/agent/types.py</files>
  <action>Create a runtime entrypoint (for example `run_agent_once(prompt: str)`) that builds or receives a LangGraph/LangChain ReAct agent bound to exactly `web_search` and `web_crawl`. Add startup-time assertion that the bound tool names match canonical Phase 2 names to prevent drift. Keep provider/model payloads internal by mapping raw runtime output to an internal `AgentRunResult` shape with at least `final_answer`, `tool_call_count`, `elapsed_ms`, `status`, and `run_id`.</action>
  <verify>Run runtime-focused tests after implementation: `pytest backend/tests/agent/test_runtime.py -q`.</verify>
  <done>Runtime executes one prompt end-to-end, uses only the two expected tools, and returns a normalized internal result object without raw provider payload leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded loop/recursion controls and explicit runtime error mapping</name>
  <files>backend/agent/runtime.py, backend/agent/prompts.py, backend/agent/types.py</files>
  <action>Enforce bounded execution by passing a `recursion_limit` (or equivalent bounded-step control) per invocation and preserving a clear stop strategy in system instructions ("stop when enough context is gathered"). Map known runtime failures (recursion/loop limit, tool call failure, model/provider failure, invalid prompt input) into explicit internal error categories so the API seam can produce stable HTTP responses in plan 03-02.</action>
  <verify>Execute targeted failure-path tests: `pytest backend/tests/agent/test_runtime.py -q -k "recursion or failure or timeout"`.</verify>
  <done>Normal runs terminate before limits, forced loop/failure scenarios produce deterministic typed runtime errors, and no unbounded execution path remains.</done>
</task>

</tasks>

<verification>
Validate with automated tests that:
- A direct-answer prompt completes in one run.
- A research prompt can call multiple tools before finalizing.
- Loop limit paths produce explicit typed runtime errors rather than hangs.
</verification>

<success_criteria>
- Runtime seam is independent of HTTP route code.
- One-call runtime function returns deterministic result envelope for downstream API mapping.
- Canonical tool names and bounded execution guarantees are enforced by code and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-execution-loop-and-api/03-01-SUMMARY.md`.
</output>
